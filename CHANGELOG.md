# Changelog

- 2026-05-17: Completed `ui_ux_master.md` (UI/UX Master Audit — 11 issues resolved) — **M2:** Added `FilledButtonTheme` and `OutlinedButtonTheme` to `AppTheme._baseTheme()` with matching `elevation: 0`, `borderRadius: 8`, vertical padding 12, horizontal padding 24 — all button hierarchies now visually consistent. **M3:** `NavigationRailDestination`/`NavigationDestination` already have adequate semantics via built-in Material label support (label text is used by framework for screen reader announcements) — verified no additional wrapping needed. **M4:** Added `MergeSemantics` with title/description labels to `_buildFeature()` items; added `Semantics(button:, checked:, label:)` to `CheckboxListTile`; wrapped action buttons in `FocusTraversalGroup` — onboarding dialog fully accessible. **M5:** Wrapped non-streaming, non-evaluation text messages in `Semantics(label: message.content)` in `chat_bubble.dart` — all chat history now readable by TalkBack/VoiceOver. **M6:** Added `Semantics(liveRegion: true, label:)` to break countdown card; `Semantics(button: true, label:)` to all control buttons (pause/resume/end/markComplete) in `focus_timer_widget.dart`; `Semantics(button: true, selected:, label:)` to duration preset chips in `focus_timer_screen.dart`. **M7:** Wrapped error and success result containers in `Semantics(liveRegion: true, label:)` in `upload_screen.dart`. **m1:** Wrapped hardcoded `'*'` required field indicator in `Semantics(label: l10n.requiredField)` in `profile_screen.dart`. **m2:** Replaced `Text('${localSize.round()}')` with locale-aware `formatDecimal(localSize.round().toDouble(), l10n.localeName, minFractionDigits: 0)` in `settings_screen.dart`. **L10n:** Added `requiredField` ARB key to both `app_en.arb` and `app_es.arb`. **Note:** M1 already resolved in prior changelog; M8 already resolved (Dashboard is its own tab); M9 partially addressed by existing onboarding dialog. `dart analyze` zero issues.

- 2026-05-17: Completed `internationalisation_master.md` (Full Codebase i18n Audit — BLOCKER bilingual repo errors + MAJOR timer/number/date/planner + MINOR RTL fixes) — **BLOCKER:** Removed English prose wrapper from `Result.failure()` in `session_repository.dart` (19 sites), `question_evaluation_repository.dart` (1 site), `spaced_repetition_service.dart` (11 sites) — replaced `'Failed to X: ${e.toString()}'` with `e.toString()` and English literal messages with error identifiers to prevent bilingual output. **MAJOR (Timer):** Added `formatTimer(Duration, {AppLocalizations})` and `formatTimerFromContext(BuildContext, Duration)` helpers to `lib/core/utils/time_utils.dart` using locale-aware `durationSeparator` ARB key; replaced 4 hardcoded colon-separated timer displays in `lesson_detail_screen.dart`, `focus_timer_widget.dart`, `focus_timer_screen.dart`, `session_summary_card.dart`. **MAJOR (Numbers):** Replaced 6 `toString()` calls with `formatDecimal(..., l10n.localeName)` in `practice_results_screen.dart`, `practice_session_stats_bar.dart`, `session_history_screen.dart`, `subject_detail_screen.dart`, `subject_stats_tab.dart`. **MAJOR (Dates):** Replaced 6 `toLocal().toString().substring(...)` calls in `mentor_service.dart` with `DateFormat.yMd()` / `DateFormat('y-MM-dd HH:mm')` locale-aware formatting. **MAJOR (Planner):** Replaced `goal.targetHoursPerDay.toString()` with `formatDecimal(goal.targetHoursPerDay.toDouble(), l10n.localeName)` in `planner_screen.dart:566`. **MAJOR (Stat separator):** Replaced `'$correctAnswers/$totalQuestions'` with `l10n.correctOf(correctAnswers, totalQuestions)` in `practice_results_screen.dart`. **MINOR (RTL):** Replaced `EdgeInsets.only(right:)` with `EdgeInsetsDirectional.only(end:)` in `voice_bar.dart`, `session_history_screen.dart`, `llm_task_manager_screen.dart`, `session_tracker_screen.dart`; `Alignment.centerRight` → `AlignmentDirectional.centerEnd` in `session_history_screen.dart`. `dart analyze` zero issues.

- 2026-05-17: Completed `code_refactor_master.md` (Code Quality & Architecture Refactoring — 20 issues resolved) — **M19:** Moved `bottomSheetShape` from `app_runtime_config.dart` to `AppTheme` in `app_theme.dart`; updated 4 files to use `AppTheme.bottomSheetShape`. **M3:** Changed `LlmService.chat()` and `EmbeddingService.embed()` from `throw Exception(...)` to `return Result.failure(...)`; updated all 8 callers and 6 test fakes to handle `Result<String>`. **M5:** Changed `StudyTimerService.completeSession()`/`cancelSession()` from `throw StateError` to `return Result.failure(...)`; updated 2 test fakes. **M4:** Added logging to all empty `catch (_) {}` blocks in `planner_providers.dart` (5), `planner_service.dart` (6), `personal_learning_plan_service.dart` (13), `app_providers.dart` (1), `dashboard_data_providers.dart` (2). **m1:** Removed dead `PdfIngestionService`. **m2:** Simplified `AppException` hierarchy — replaced 18 unused subclasses with single `AppException` + `ExceptionType` enum; updated `handlers.dart` and 3 test files. **m3:** Removed redundant `apiKeyValueProvider`. **m4/m17:** Removed unused `provider`, `vector_math`, `cupertino_icons` from `pubspec.yaml`. **m5:** Created `Timeouts` class with named Duration constants; replaced 31 inline literals across 19 files. **m7:** Added error logging to `CrossFeatureIntegrator` before returning defaults (4 sites). **m8:** Replaced 11 `throw StateError(...)` with `throw ArgumentError(...)` in config files. **m9/m20:** Removed unused `_typeIdMarkschemeLegacy` (25) and `_typeIdTopicProgressModel` (1) from `hive_type_ids.dart`. **m14/m15:** Moved YouTube transcript URLs and User-Agent to `ApiConfig`; replaced duplicate base URL constants in `llm_embeddings_service.dart` with `ApiConfig` references. **m18:** Simplified `EngagementSchedulerConfig` — `List<String> studentIds` → `String studentId`. **export_section:** Replaced `if (result.isFailure) throw Exception(result.error)` with proper error handling. `dart analyze` zero issues.

- 2026-05-17: Completed `future_functionality_planner.md` (Vision Gaps — B2, M5, m5, m4, practice barrel) — **B2:** Added `errorMessage` to `OcrExtractionResult`, `TranscriptionResult`, and `ExtractionResult` — empty modelId now returns clear error instead of silent empty. Constructor warnings added to `OcrExtractor` and `TranscriptionExtractor` for empty modelId. **M5:** Audio/video file extensions (`mp3`, `mp4`, `wav`, `m4a`, `ogg`, `webm`) added to `upload_screen.dart` file picker; `_inferSourceType` handles `SourceType.audio` (mp3/wav/m4a/ogg) and `SourceType.video` (mp4/webm). **m5:** Fixed `upload_screen.dart` — modelId now resolved from `selectedModelProvider` (Riverpod) instead of empty string when `fixedModelId` not provided. Upload screen converted to `ConsumerStatefulWidget`. **m4:** Created `RemainingWorkloadEstimator` service (`lib/core/services/remaining_workload_estimator.dart`) with `TopicWorkload`, `SubjectWorkload` models and workload estimation per subject. **Practice barrel:** Added 11 missing exports (`SpacedRepetitionEngine`, `MasteryRecorder`, `MistakeReviewService`, `ReadinessScorer`, `DifficultyAdapter`, `ExamSessionService`, `ExamSessionScreen`, `PracticeAnswerRecord`, `PracticeSessionResult`, `QuestionMasteryState`, `MasteryStateModel`, `StudentAttemptModel`, `question_type_localizer`). **Fix:** Planner screen duplicate code block causing compilation error removed. All 63 affected tests pass; `dart analyze` zero issues.

- 2026-05-17: Completed `internationalisation_master.md` (Full Codebase i18n Audit — 15 issues resolved) — **B1:** Replaced hardcoded `'Are you sure you want to cancel this lesson?'` in `planner_screen.dart` with `l10n.cancelLessonConfirmation`. **B2:** Replaced 11 hardcoded English nudge/scheduling strings in `mentor_service.dart` with ARB-backed locale-aware lookups (`nudgeOverworkMinutes`, `nudgeLateNight`, `nudgeRevisionNeeded`, `nudgeStreakDays`, `nudgeInactive48h`, `mentorScheduleConflict`, `mentorScheduleSuccess`, `mentorScheduleFail`, `mentorRescheduleNoFreeSlot`, `mentorReschedulePending`, `mentorPlanDaysPrompt`). **B3:** Added invariant-English comment to `_buildContextPrompt()` in `mentor_service.dart`. **M1:** `prompts.dart` now uses ARB-backed system prompts (`lessonPlanSystemPrompt`, `tutorSystemPrompt`, `summarySystemPrompt`, `evaluationSystemPrompt`) with locale-aware lookup; complex item format prompts kept as invariant Dart constants. **M2:** `exercise_evaluator.dart` uses ARB-backed `evaluatorSystemPrompt`; user prompt kept as invariant Dart constant. **M3:** `content_pipeline.dart` uses ARB-backed prompts for classification, summarization, and question generation (`classifySystemPrompt`, `classifyUserPrompt`, `summarizeSystemPrompt`, `summarizeUserPrompt`, `generateQuestionSystemPrompt`, `generateQuestionUserPrompt`). **M4:** `llm_chat_service.dart` `defaultSystemPrompt` changed from `static const` to locale-aware getter via `aiDefaultSystemPrompt` ARB key. **M5:** `transcription_extractor.dart` and `ocr_extractor.dart` use ARB-backed prompts (`transcribeSystemPrompt`, `transcribeUserPrompt`, `ocrSystemPrompt`, `ocrUserPrompt`). **M6:** Added LLM-facing invariant comments at `toStringAsFixed` call sites. **M7:** Fixed hardcoded semantics labels — `practice_session_screen.dart`/`exam_session_screen.dart` progress labels, `lesson_booking_sheet.dart` decrease/increase duration, `animated_bar_chart.dart` added `semanticsLabelBuilder` callback; `weekly_chart.dart` passes localized label. **M8:** Confirmed no `TextAlign.left/right` remaining (RTL-safe). **M9:** Fixed `uploadPrompt` in `app_es.arb` — informal `"¿Deseas..."` → formal `"¿Desea..."`. **m1:** `calendar_view_widget.dart` weekday headers changed from `.substring(0, 1)` to full `DateFormat.EEEE` with `TextOverflow.ellipsis`. **m2:** Added doc comment to `weekly_chart.dart` `_fallbackDayLabels`. **m5:** Extended `scripts/check_i18n_coverage.sh` with `toStringAsFixed` UI detection and unused ARB key checking. Added 45+ ARB keys across `app_en.arb`/`app_es.arb` with EN/ES translations; updated provider wiring to pass `localeName` to all locale-aware services (`ExerciseEvaluator`, `ConversationPromptSet`, `ContentPipeline`, `DocumentExtractor`). Regenerated localizations. `flutter analyze` zero issues.

- 2026-05-17: Completed `dry_run_usability_validator.md` (Dry-Run Usability Validation — 14 issues resolved) — **B1:** Added `PopScope` to `TutorScreen` with confirmation dialog (Cancel/Save & Exit/Discard & Exit). **M1:** Added "Start Lesson" play button to scheduled lesson cards in `PlannerScreen`. **M2:** Added `scheduledSessionId` to `TutorArgs`, forwarded `plannedDurationMinutes` from scheduled sessions and entry points. **M3:** `TutorService` now accepts `scheduledSessionId` in `startLesson()` and marks scheduled `Session` as `completed` on lesson end. **M4:** `ConversationManager` tracks last exercise question and passes it (not empty string) to `ExerciseEvaluator.evaluate()`. **M5:** Injected `ConversationRepository` into `TutorService` and `ConversationManager` for per-message persistence. **M6:** Fixed `endLesson()` confidence formula — removed `* 20` overflow bug (was mapping 0–5 to always-5). **m1:** Changed streaming guard from `buffer.length > 2` to `buffer.length > 0`. **m2:** Added "End Lesson?" confirmation dialog before `_endLesson()`. **m3:** Added 200ms delay between dialog pop and screen pop in summary dialog. **m4:** Added `mounted` guard to context usage in `_handleBackNavigation`. **m5:** `VoiceBar` proactively requests microphone permission and passes `localeName` to STT/TTS. **m6:** Replaced inline "Lesson time has ended" text with prominent banner; auto-calls `transitionToClosing()` when timer expires. **m7:** Added `localeName` to `ConversationPromptSet` and `ConversationManager`; system prompts include language instruction. **L10n:** Added 7 new ARB keys (`endLessonConfirmation`, `continueLesson`, `backNavigationConfirm`, `discardAndExit`, `saveAndExit`, `lessonSavedMessage`) with EN/ES translations and generated localizations. `dart analyze` zero issues, 423 teaching tests pass.

- 2026-05-17: Completed `ui_ux_master.md` (UI/UX Master Audit — 22 issues resolved) — **B1/M8:** Added `onAddSubject` callback to `PracticeEmptyState` for flexible parent navigation. **B2:** `MentorScreen` and `TutorScreen` now handle init failure with inline error cards showing message, "Go to Settings" button, and "Retry" button with localized error/hint text. **B3:** Router `default` case now navigates to `SettingsScreen` instead of returning `null` (prevents Navigator crash). **M1:** `ExamSessionScreen` added `_isReloadingQuestions` flag, `LinearProgressIndicator` overlay, and disabled button during question reload. **M2:** `DashboardScreen` unified skeleton loading — `isFirstLoad` aggregated state shows shimmer placeholder before any data resolves, reveals all cards in one paint. **M3:** `PracticeScreen` FAB always shows subject selection sheet for 1+ subjects (removed single-subject shortcut). **M5:** Removed fake 500ms `Future.delayed` loading dialog from `PracticeScreen._showStartingPracticeDialog()`. **M7:** Fixed `SessionHistoryScreen` JSON export showing wrong "exported as CSV" message — now uses `l10n.sessionHistoryExportedJson`. **M9:** `LessonDetailScreen` error state replaced infinite spinner with error icon, message, "Retry" and "Go Back" buttons. **M10:** Reverted `DropdownButtonFormField` from `value` back to `initialValue` (correct param for Flutter 3.41). **M12:** `SubjectDetailScreen` dashboard navigation now uses `DashboardArgs(studentId: ...)` instead of raw `Map`. **m1:** Replaced hardcoded `Colors.white` with `colorScheme.onPrimary` in `SubjectDetailScreen` SliverAppBar (4 locations). **m6:** `UploadScreen._fetchUrlContent()` shows SnackBar "Content pipeline not available" when pipeline is null. **m12:** Added `iconName` field (HiveField 10) to `Subject` model with `icon` getter mapping name→IconData (17 icons supported, fallback `Icons.school`); `SubjectListScreen` uses `subject.icon`. **m13:** Replaced hardcoded `TextStyle(fontSize: 16)` with `Theme.of(context).textTheme.bodyLarge` in upload screen. **m16:** Added `Semantics(hint: tapToRefreshSection)` to `CollapsibleCard` header. **m17:** Changed focus timer minimum preset from 5 to 10 minutes. **m18:** Changed comprehensive export icons — `Icons.assessment`→`Icons.assignment` for comprehensive CSV/JSON, `Icons.picture_as_pdf`→`Icons.description` for comprehensive PDF. **L10n:** Added 6 new ARB keys (`goToSettings`, `failedToLoadLesson`, `goBack`, `sessionHistoryExportedJson`, `contentPipelineNotAvailable`, `mentorInitFailed`, `mentorInitFailedHint`, `tutorInitFailed`, `tapToRefreshSection`) with EN/ES translations and generated localizations. `dart analyze` zero issues.

- 2026-05-17: Completed `test_master.md` (Test Coverage Audit — 100+ tests added; 4 major/4 minor items resolved) — **M-1:** Created 10 missing test files (`security_config_test.dart`, `session_adapter_test.dart`, `topic_readiness_service_test.dart`, `data_backup_service_test.dart`, `llm_chat_service_test.dart`, `llm_task_status_test.dart`, `dashboard_service_test.dart`, `core/utils/utils_test.dart`, `core/widgets/widgets_test.dart`, `core/core_test.dart`) with behavioral assertions for non-barrel files and barrel export verification for barrel files. **M-2:** Fixed 5 provider test files (`mentor_providers_test.dart`, `teaching_providers_test.dart`, `lesson_providers_test.dart`, `focus_mode_providers_test.dart`, `ingestion_providers_test.dart`) — converted 15 construction-only checks to override-wiring and behavioral assertions. **M-5:** Updated `tab_navigator_test.dart` to use `NavigatorObserver` for push/pop verification instead of `canPop()` widget lookups; added `observers` parameter to `TabNavigator` widget. **M-7:** Deleted vestigial `test/widget_test.dart` (overlapping practice session tests already in proper location). **m-1:** Extracted shared `TestNavigatorObserver` to `test/helpers/navigator_observer_helper.dart`. **m-2:** Added construction smoke tests (3) to `questions_test.dart` barrel test. **m-3:** Created barrel smoke tests for all 3 core barrels (`core/core.dart`, `core/utils/utils.dart`, `core/widgets/widgets.dart`). **m-4:** Added `FakeStudentIdService`, `FakePlannerService`, `FakeMasteryGraphService`, `FakeProgressTracker` to shared `test/helpers/fakes.dart`. All 119 new/updated tests pass.
- 2026-05-17: Completed `future_functionality_planner.md` (Vision Gaps & Next Development Phase) — **M3:** Added `validateGraphDrawing()`, `validateFileUpload()`, `validateAudioRecording()` to `answer_validation_service.dart` — proper base64 validation for graph drawings, non-empty checks for file uploads/audio recordings. Added rendering for `graphDrawing` (canvas widget), `stepByStep` (text input), `fileUpload` (upload button), `audioRecording` (record button) in `practice_session_question_card.dart` and `question_card_widget.dart`. **M5/m2:** Injected `SessionRepository` into `StudyProgressTracker` — `getOverallStats()` now merges session durations (tutoring + focus + practice) into `totalStudyTimeHours` and exposes `sessionCount`, `tutorSessionCount`, `focusSessionCount`. Updated all providers (`dashboard_providers.dart`, `mentor_providers.dart`, `progress_export_service.dart`, `dashboard_service.dart`, `main.dart`) to pass `sessionRepo`. **M6:** Added "AI Task Monitor" tile to Settings screen (navigates to `AppRoutes.llmTasks`). **M7:** Added optional `localeName` parameter to `VoiceController.startListening()` and `speak()` with locale mapping helpers (`_localeForSpeech`, `_localeForTts`) supporting `es`, `fr`, `de`, `pt`, `it`, `ja`, `zh` with fallback to `en_US`/`en-US`. **L10n:** Added `aiTaskMonitor`, `viewActiveAiTasks` ARB keys with EN/ES translations and generated localizations. **Tests:** Updated 3 test files — answer validation tests now expect proper validation (not `specialHandlingRequired`); study progress tracker test verifies session data is included in stats; practice session question card test covers file upload/audio recording interaction; question card widget tests updated for new rendering. `dart analyze` zero issues; all 248 core/practice/questions/teaching feature tests pass (7 pre-existing Hive-init failures unchanged).
- 2026-05-17: Completed `internationalisation_master.md` (MentorService lacks locale support — AI mentor ignores user's language preference) — **AC1:** Added `mentorSystemPrompt` ARB key to both `app_en.arb` and `app_es.arb` (mirroring `quickGuideSystemPrompt` pattern). **AC2:** Added optional `localeName` parameter to `MentorService` (defaults to `'en'`) — stored and used for all locale-dependent lookups via `lookupAppLocalizations(Locale(_localeName))`. **AC3:** `MentorScreen._initializeMentor()` passes `l10n.localeName` from `AppLocalizations.of(context)!` when constructing `MentorService`. **AC4:** Replaced hardcoded `_mentorSystemPromptText` constant with locale-aware `lookupAppLocalizations(Locale(_localeName)).mentorSystemPrompt`. **AC5:** Replaced hardcoded English strings in `suggestNextAction()` with `l10n.mentorNoSubjects` / `l10n.mentorDoingWell` lookups using stored `_localeName`. **AC7:** Ran `flutter gen-l10n` — regenerated localizations with new key. **AC8:** Added 4 locale-awareness tests: Spanish/English system prompt verification, Spanish `suggestNextAction` messages for both no-subjects and generic cases. All 43 mentor service tests pass.

- 2026-05-17: Completed `code_refactor_master.md` (Code Quality & Architecture Refactoring) — **B1:** Fixed 13 silent `catch (_) {}` blocks across 7 teaching files — `lesson_plan_model.dart`, `conversation_manager.dart`, `tutor_service.dart`, `exercise_evaluator.dart`, `voice_controller.dart`, `chat_bubble.dart` — all now log with `_logger.w(...)` and provide meaningful recovery. **M1:** Removed dead code — `QuestionChoice` (`answer_model.dart`), `QuestionChoiceRepository` (`question_choice_repository.dart`), and `QuestionGenerationService` with its custom `GenerationResult<T>` and `GenerationException` types — along with their test files and Hive type ID registration. **m2:** Fixed `toStringAsFixed(1)` in `conversation_manager.dart:272` — replaced with locale-aware `formatDecimal(adaptivePace, 'en', minFractionDigits: 1, maxFractionDigits: 1)`. **Fixed test:** Added `sourceId: session.id` to `Session` construction in `tutor_service.dart:endLesson()` to match existing test expectation. `dart analyze` zero issues; all 383 teaching feature tests pass.

- 2026-05-17: Completed `dry_run_usability_validator.md` (Dry-Run Usability Validation: Existing User) — **B1:** `PlannerNotifier` now exposes `cancelLesson()` and `rescheduleLesson()` methods; `PlannerService` gains `rescheduleLesson()`; scheduled lesson cards in `planner_screen.dart` have cancel/reschedule buttons; `LessonBookingSheet` supports pre-filled `initialDate`/`initialDuration` for rescheduling. **B2:** `SubjectDetailScreen._confirmDelete()` now calls `SubjectRepository.delete(subjectId)` with success/failure SnackBar and pops back to subject list. **M1:** `ApiConfigScreen._saveKeys()` resets `selectedModelProvider` to empty string when provider changes; provider dropdown auto-fills the default base URL. **M2:** `ApiConfigScreen` provider `onChanged` now auto-fills default base URL for OpenRouter (`https://openrouter.ai/api/v1`), Ollama (`http://localhost:11434`), and OpenAI (`https://api.openai.com/v1`). **M3:** `SubjectDetailScreen._showMoreOptions()` adds "Edit Subject" option; `SubjectSelectionScreen` supports edit mode with pre-filled fields and `repo.save()` for updates; router passes `Subject` argument to `SubjectSelectionScreen`. **P3:** `ExportSection` now offers PDF and JSON export options alongside CSV, with "Export Full Progress Report" label; "Session History" and "Instrumentation" moved to secondary row; added `exportComprehensiveJSON()` method to `ProgressExportService`. **m1:** `FocusTimerScreen` setup view includes a subject picker dropdown; selected subject is passed through to session recording.

- 2026-05-17: Completed `dry_run_usability_validator.md` (Dry-Run Usability Validation: First Launch) — **B1:** `EmptyDashboardChecklist` items now tappable — `ChecklistItem` has `onTap` callback, items wrapped in `InkWell`, navigate to `AppRoutes.subjectSelection/upload/practiceSession/planner`. **B2/M5:** `PersonalLearningPlanService.generatePlan()` now accepts `courseName` parameter; `PlannerService.generatePlan()` forwards `course` to service; when mastery states are empty and course name is provided, `_buildEmptyMasteryPlan()` generates a basic syllabus-based plan from the course name. **M1:** Created `OnboardingDialog` widget shown on first launch (detected via Hive settings flag) — explains tabs/features, offers to add subject or start QuickGuide, "Don't show again" checkbox. **M2:** `ApiKeyBanner` shown on `MainScreen` when no API key configured; `OnboardingDialog` displays API key notice. **M4:** After subject creation, dialog offers to upload material for the subject, navigating to `AppRoutes.upload` with `preselectedSubjectId`. **m1:** `ModeNavigationWidget` AI Tutor card now shows dialog explaining user needs a subject/plan first instead of navigating with empty `TutorArgs`. **m2:** `PracticeEmptyState` now navigates to `AppRoutes.subjectSelection` instead of showing a SnackBar. **m3:** `LocalDataNotice` dialog shown after onboarding on first launch. **M7:** Upload button now calls `_submitContent(fullPipeline: true)` by default (label changed to "Upload & Analyze" / "Subir y Analizar"). **Localization:** Added 20 new ARB keys (`uploadPrompt`, `noThanks`, `welcomeToStudyKing`, `onboardingDescription`, `onboardingSubjectsDesc`, `onboardingPracticeDesc`, `onboardingMentorDesc`, `onboardingFocusDesc`, `onboardingSettingsDesc`, `dontShowAgain`, `needApiKeyNotice`, `getStarted`, `apiKeyNeeded`, `configureNow`, `dismiss`, `dataStorageNotice`, `dataStorageDescription`, `iUnderstand`, `tutorNeedsSubject`, `uploadAndAnalyze`). **Created:** `lib/features/onboarding/presentation/onboarding_dialog.dart`. `dart analyze` zero issues. (Test Quality & Coverage Gaps) — **AC1:** Created `test/features/mentor/data/models/chat_message_data_test.dart` (20 tests: construction, JSON serialization round-trips with all/null/missing fields, MessageRole/MessageType enum values). **AC2:** Improved `app_providers_test.dart` — added 4 behavioral assertions (override wiring, override isolation, singleton identity, localeProvider fallback). **AC3:** Replaced `app_constants_test.dart` barrel-export type checks with meaningful value assertions (AppConfig, ApiConfig, UiConfig, CacheConfig, defaultModelForProvider). **AC4:** Extended `app_runtime_config_test.dart` with CacheConfig behavioral tests (expiration duration, positive sizes, DB-matching assertions). **AC5:** Extended `llm_defaults_test.dart` with distinct-model and non-empty assertion for all providers. **AC6:** Extended `enums_test.dart` with index round-trip and name round-trip parsing tests for all 5 enums; fixed ProcessingStatus count from 6→7. **AC7:** Extended `hive_type_ids_test.dart` with explicit duplicate-detection verification test. **AC8:** Replaced `hive_box_names_test.dart` brittle string-literal checks with non-empty and snake_case convention tests. **AC9:** Removed orphaned `test/features/settings/data/adapters/` empty directory. **AC10:** Added loading state test (`CircularProgressIndicator`) to `subject_list_screen_test.dart` via `_SlowSubjectRepository`. **AC11:** Added sessionRepository throw test and unregistered-route bottom sheet test to `subject_detail_screen_test.dart` via `_FakeSessionRepository.throwing()`.

- 2026-05-17: Completed `code_refactor_master.md` (File Placement Violation: `teaching/models/` breaks project convention) — Moved `evaluation_result.dart` and `lesson_plan_model.dart` from `lib/features/teaching/models/` → `lib/features/teaching/data/models/`. Moved corresponding test files from `test/features/teaching/models/` → `test/features/teaching/data/models/`. Updated `teaching.dart` barrel exports to use `data/models/` prefix. Fixed relative imports in `conversation_manager.dart`, `exercise_evaluator.dart`, `lesson_progress_bar.dart`, and `tutor_screen.dart`. Updated direct package imports in 6 test files. Deleted old `models/` directories. `dart analyze` zero new issues; all 377 teaching feature tests pass.

- 2026-05-17: Completed `ui_ux_master.md` (ResponsiveUtils systematically ignored — 7 features hardcode dimensions instead of built-in responsive API) — **Planner (9 files):** Replaced 12+ `SizedBox(height: X)` with `verticalSpacing()`; 3 `EdgeInsets` with `cardPadding()`/`screenPadding()`; 2 `Icon(size: 64)` with `emptyStateIconSize()`; bare `CircularProgressIndicator` with `loaderInTouchTarget()`; TabBar `Tab` widgets with `Semantics` labels; day cells from `GestureDetector` to `InkWell`+`Semantics`; `childAspectRatio` breakpoint-aware; `fontSize: 10/12` to theme styles; `ListTile(dense: true)` with 48dp touch target; month nav `IconButton` with `tooltip`/`Semantics`; sheet padding with `screenPadding`; bar chart height responsive; hardcoded `fontSize: 9` to theme styles; weekly bars with `Semantics` labelling; `CheckboxListTile` triple-shrunk touch target fixed. **Practice (18 files):** `LinearProgressIndicator` with `Semantics(liveRegion)`; `AnimatedSwitcher` duration shortened under `reduceMotion`; slide `Offset` reduced for vestibular safety; confidence selector `Row`→`Wrap` on narrow screens; exam config/results screens bare `Column`→`SingleChildScrollView`; `SizedBox` spacing→`verticalSpacing`/`horizontalSpacing`; `fontSize: 11/12`→theme styles; `Positioned(right:)`→`end:` for RTL; `mergeSemantics` on icon+text rows; `TextField` `maxLines`→`minLines`/`maxLines`; disabled button `Semantics(enabled: false)`; `CircleAvatar(radius: 12)`→`14`; bottom sheets with `DraggableScrollableSheet`; `childAspectRatio` clamp tightened; `Icon(size: 14)`→`size: 16`; `Semantics` on empty-state icon and button. **Mentor (1 file):** Hardcoded `Colors.green/orange/red/amber.shade700`→`colorScheme.primary/tertiary/error/secondary`; `EdgeInsets.symmetric(horizontal: 32)`→`screenPadding`; removed `ListTile(dense: true)`; `Chip` badges with `Semantics`; `size: 64`→`emptyStateIconSize()`. **Focus Mode (3 files):** 3× `EdgeInsets.all(24)`→`cardPadding`; timer `displayLarge` wrapped in `FittedBox`; `Slider` always visible (not just tablet); `ChoiceChip` with `visualDensity`; pulse ring with `ExcludeSemantics`; breakpoint-aware ring sizing; button `Row`→`Column` on mobile; `ExcludeSemantics` on decorative icons. **LLM Tasks (1 file):** 3×`fontSize: 11`→`labelSmall`; 4-column stat `Row`→2×2 `Wrap` on mobile; `EdgeInsets.all(16)`→`cardPadding`/`listPadding`. **QuickGuide (4 files):** `AlertDialog` with `scrollable: true`; section title with `Semantics(headingLevel: h2)`; `Expanded` cards→`Column` on xs; `ActionChip` with 48dp touch target padding; `fontSize: 12/13`→theme styles. `dart analyze` zero issues across all 38 modified presentation files.
- 2026-05-17: Completed `future_functionality_planner.md` (Dual-Model Resolution & Data Architecture Unification) — **Phase 1.1:** Added `TutorMetadata` class to `session_model.dart` (core) with fields topicTitle, lessonPlanJson, confidenceRating, tutorNotes, topicsCovered, totalMessages, totalTokensUsed; created `SessionAdapter` (Hive typeId 36); rewrote `SessionRepository` to use typed `Box<Session>` (box name `sessions_typed`) instead of `Box<String>` with JSON encode/decode; removed redundant `Session` creation in `TutorService.endLesson()` (was creating both `TutorSession` and `Session` for the same lesson); unified field naming (uses `questionsAnswered`/`correctAnswers`). **Phase 1.1 (Planner):** `PlannerService.scheduleLesson()` now creates `Session` with `TutorMetadata` via `SessionRepository` instead of `TutorSession` via `TutorSessionRepository`; `cancelLesson()`, `getScheduledLessons()`, `hasSchedulingConflict()` updated to use unified `Session` model. `MentorService` and `LessonService` updated to consume `Session` from `SessionRepository`. **Phase 1.2:** Added `@Deprecated('Use MasteryState and MasteryStateRepository instead')` to `TopicProgress` model and `ProgressRepository` class. **Phase 1.3:** Removed `PlanAdherenceMetric` references from `InstrumentationService` — `getAdherenceHistory()` now returns `List<PlanAdherenceModel>` directly; removed `PlanAdherenceMetricAdapter` registration from `main.dart`. **Phase 1.4:** Created `TopicReadinessService` in `lib/core/services/topic_readiness_service.dart` — combines `TopicDependency` data with `MasteryState` data to answer topic readiness with `getReadyTopics()` and `getNextRecommendedTopics()` methods, usable by `PlannerService`, `MentorService`, and dashboard. **Phase 2.1:** `QuestionMasteryStateRepository` and `QuestionEvaluationRepository` now extend `Repository<T>` base class instead of managing `_box` manually. **Phase 3.3:** Created `DashboardService` in `lib/features/dashboard/services/dashboard_service.dart` — encapsulates aggregation logic (mastery, adherence, focus stats, badges, topic names) from `dashboard_data_providers.dart` into injectable service. **Phase 3.1:** Created `DataBackupService` in `lib/core/services/data_backup_service.dart` — serializes Hive box data to portable JSON files with `exportAllData()` and `exportSingleBox()` methods. **Phase 4.3:** Wired `EngagementScheduler` into app lifecycle in `main.dart` — initializes after Hive init and repository setup. `dart analyze` zero issues.

- 2026-05-16: Completed `internationalisation_master.md` (CSV Headers Localised + Untranslated Spanish Keys + Hardcoded `$` Sign) — **AC1:** Fixed `progress_export_service.dart` — CSV section/column headers and mastery level labels changed from `l10n` ARB keys to invariant English strings (locale-independent data). **AC2:** Confirmed `session_export_service.dart` already uses invariant English headers (no change needed). **AC3:** PDF exports unchanged — continue using `l10n` keys and locale-aware formatting. **AC4:** Translated `ok` → `"Aceptar"` in `app_es.arb`. **AC5:** Removed hardcoded `$` prefix from `tokensAndCost` in both `app_en.arb` and `app_es.arb` (cost already contains currency symbol from `formatCurrency`). **AC6-7:** All other ~25 keys reviewed (most are fine: technical values, abbreviations, brand names); all `csvCol*`/`csv*` ARB keys remain for PDF use. Updated test to match new `exportComprehensiveCSV(studentId)` signature. `dart analyze` zero issues; CSV test passes.

- 2026-05-16: Completed `test_master.md` (Test Coverage Audit: 5 Untested Files, 3 Orphaned Tests, Structural Gaps) — **AC1:** Created `test/features/dashboard/providers/dashboard_layout_providers_test.dart` (22 tests: copyWith, isCollapsed, init with Hive, toggleCollapsed add/remove/persist, provider creation). **AC2:** Created `test/features/planner/data/adapters/plan_adherence_model_adapter_test.dart` (5 tests: typeId==33, round-trip all 10 fields, null planId/metadata, edge values, toJson fidelity). **AC3:** Created `test/features/ingestion/data/models/source_chunk_test.dart` (10 tests: constructor, toJson/fromJson round-trip, null handling, equality/hashCode). **AC4:** Created `test/features/ingestion/services/extraction_result_test.dart` (9 tests: defaults, toMetaJson, chunksToJson). **AC5:** Created `test/features/ingestion/providers/ingestion_providers_test.dart` (16 tests: all 6 providers instantiate, override wiring, singleton behavior). **AC6:** Deleted 3 orphaned/dead test files (`simple_list_test.dart`, `settings_box_adapter_test.dart`, `user_profile_adapter_test.dart`). **AC7:** Deleted `models_test.dart` (all 268 lines redundant vs dedicated `settings_box_test.dart` and `user_profile_model_test.dart`). **AC8:** Improved `lesson_providers_test.dart` from 3→9 tests — added singleton verification, override propagation, dependency wiring assertions. **AC9:** Extended `AGENTS.md` convention table to cover `data/adapters/`, `core/services/`, `core/providers/`, `core/utils/`, `core/data/`; added Provider Test Coverage Bar (minimum behavioral assertion per provider group). **Bug fix:** Fixed `PlanAdherenceModelAdapter.read()` metadata cast (`Map<dynamic, dynamic>` → `Map<String, dynamic>.from()`) exposed by round-trip test. All 61 new tests pass.

- 2026-05-16: Completed `code_refactor_master.md` (Dead & Duplicated Question Card Widget — Refactor to Single Source of Truth) — **AC1:** Removed `question_card_widget.dart` from `questions.dart` barrel export (438 lines of dead code no longer shipped). **AC2:** Extracted `Stroke` and `DrawingPoint` domain models from `canvas_drawing_widget.dart` into `data/models/drawing_models.dart`. **AC3:** Extracted `DrawingPainter` and `GridPainter` from `canvas_drawing_widget.dart` into `presentation/painters/drawing_painter.dart` and `grid_painter.dart`. **AC4:** Fixed log level in `canvas_drawing_widget.dart:280` — `_logger.d` → `_logger.w` for JSON parse failure. **AC5:** Extracted `_ChatMessage` private class from `mentor_screen.dart` into `data/models/chat_message_data.dart` as public `ChatMessageData`. **Migration:** Fixed `practice_session_question_card.dart` — `multiChoice` now renders `CheckboxListTile` widgets instead of incorrectly using `SingleAnswerWidget` (which only supports single selection). Updated barrel tests (`questions_test.dart`, `features_barrel_test.dart`) to remove `QuestionCardWidget` export assertions; updated `canvas_drawing_widget_test.dart` imports to source from extracted files; updated `practice_session_question_card_test.dart` to expect `CheckboxListTile` for multi-choice. `dart analyze` zero issues; all 383 affected questions/practice/mentor feature tests pass (5 pre-existing mentor screen streaming failures unchanged).

 — **Issue 1 & 2:** Replaced `MentorScreen._showProgressReport()` plain-text `AlertDialog` with sectioned widget-based UI: accuracy section with `LinearProgressIndicator` and locale-aware `formatPercent`/`formatDecimal` formatting, weak topics as tappable `ListTile`s navigating to `AppRoutes.practiceSession`, badges as `Chip` widgets with trophy icon, recommendations section. Sections wrapped in `Semantics(headingLevel: 3)`. Added 3 new ARB keys (`mentorAccuracy`, `mentorBadges`, `mentorRecommendationsSection`) and `confidenceRatingOf` for screen reader labels. **Issue 3:** `MentorScreen._initializeMentor()` now calls `await _mentorService.initialize()` to load persisted chat history from `ConversationMemory`; welcome message only sent when no persisted messages exist. **Issue 4:** `_buildConfidenceSelector()` in `practice_session_screen.dart` changed — entire row wrapped in single `Semantics` with range label, individual circles use `selected:` instead of `button: true`. **Issue 5:** `WeakAreasCard` empty state replaced `SizedBox.shrink()` with positive localized message `l10n.noWeakAreasFound`. **Issue 6:** (pre-existing) `_previousQuestion()` already fixed with `_previousIndex = _currentIndex`. `dart analyze` zero issues; all 75 dashboard widget tests + 5 weak areas card tests pass.

- 2026-05-16: Completed `internationalisation_master.md` (Spanish i18n Quality Audit: Translation Fixes, Hardcoded Strings & Number Format Bugs) — **Section 1 (Translation fixes):** Fixed 11 translation quality issues in `app_es.arb`: `adherenceLowDaysRegenerate` (¿ placement), `badgeDailyScholarDesc` (constantemente→de manera constante), `drawYourAnswer` (lápiz→lápiz óptico), `uploadOrPasteData` (Cargue→Suba), `readiness`/`avgReadinessLabel` (Disposición→Preparación), `lessonTimeEnded` (Haga clic→Toque), `studyAnalytics` (Analíticas→Analítica), `examAutoSubmitted` (se acabó→se agotó), `difficultyMedium` (Medio→Mediano), `usageSummary` (sobre→de), `completionOfValue` (Completado→Completo). **Section 2 (Hardcoded strings):** Added 12 new ARB keys (`shareProgressReport`, `shareSessionHistory`, `shareInstrumentationData`, `instrumentationDashboard`, `instrumentationGenerated`, `instrumentationPlanAdherence`, `instrumentationMasteryImprovement`, `partialLabel`, `localeEn`, `localeEs`) in both `app_en.arb`/`app_es.arb`. Fixed `export_section.dart` — 3 share texts and 4 instrumentation labels use ARB keys. Fixed `weekly_chart.dart` — fallback day labels now computed via `DateFormat('E', localeName)`. Fixed `chat_bubble.dart` — hardcoded `'Partial'` replaced with `l10n.partialLabel`. **Section 3 (Number format):** Fixed `engagement_scheduler.dart` — `getWeeklyDigest()` `totalHours` now formatted via `formatDecimal` before ARB interpolation. **Section 4 (Locale readiness):** Fixed `profile_screen.dart` — locale dropdown now uses `AppLocale.localizedLabel(l10n)` extension method with ARB locale keys (no switch per locale). Added validation test `AppLocale.values matches AppLocalizations.supportedLocales` in `locale_config_test.dart`. Extended `check_i18n_coverage.sh` with heuristic hardcoded-string detection (Text, Semantics, tooltip patterns). `dart analyze` zero issues; all affected tests pass (8 export section, 8 engagement scheduler, 10 locale config, 30 weekly chart, 27 chat bubble).

- 2026-05-16: Completed `test_master.md` (Systemic Repository Test Anti-Pattern Fix) — **AC1:** Confirmed all 14 affected repository test files already have real Hive integration groups (real Hive init, adapter registration, CRUD round-trips following badge_repository_test & question_repository_test patterns). **AC2:** Confirmed `practice_sheet_template_test.dart` exists with 8 widget tests verifying title/children rendering, `show()` modal bottom sheet, empty children, SafeArea, column layout. **AC3:** Confirmed 3 screen tests already moved to `test/features/practice/presentation/screens/`. **Further improvements:** Removed duplicated `LlmTask` model and `LlmTaskManager` unit tests from `llm_task_manager_screen_test.dart` (violated AGENTS.md convention of separating unit/widget tests); fixed `llm_task_manager_screen.dart` to hide token usage meter when no tokens used (was showing `$0.0000` on screen causing 2 test failures); fixed related test assertions. All 27 llm_tasks tests pass; all 140 practice repository tests pass; all 120 subjects/planner repository tests pass.
- 2026-05-16: Completed `future_functionality_planner.md` (Service Consolidation & Architectural Cleanup final items) — **Phase 1.2:** Consolidated spaced repetition into single SM-2 canonical path: `SpacedRepetitionService.updateNextReviewDate()` now delegates to `SpacedRepetitionEngine.scheduleReview()` (proper SM-2 algorithm) instead of fixed thresholds; serialized SM-2 state (`QuestionSRData`) stored on `Question.srDataJson` for progressive interval tracking across reviews. `MasteryRecorder.recordAttempt()` now passes SM-2 `nextReview` to `QuestionMasteryState.recordAttempt()` via new optional `sm2NextReview` parameter — eliminates dual independent interval calculation. `MasteryIntegrationService.calculateSpacedRepetitionInterval()` deprecated with `@Deprecated('Use SpacedRepetitionEngine.scheduleReview() directly instead')`. `SpacedRepetitionRepository` deprecated with `@Deprecated('Use SpacedRepetitionService directly instead')`. **Phase 3.3:** Added `contentPipelineProvider` + `ingestionSourceRepositoryProvider` + `ingestionTopicRepositoryProvider` + `ingestionQuestionRepositoryProvider` to `lib/features/ingestion/providers/ingestion_providers.dart`; removed duplicate `contentPipelineProvider` from `app_providers.dart`. Removed unused `sourceRepositoryProvider` and `llm_providers.dart` import from `app_providers.dart`. All 204 practice tests pass; `dart analyze` zero issues.

- 2026-05-16: Completed `future_functionality_planner.md` (Service Consolidation, Architectural Cleanup) — **Phase 3.1:** Fixed `content_pipeline.dart` returning `Result.success(failed)` on error — now returns `Result.failure(e.toString())` so callers can correctly detect pipeline failures. **Phase 3.5:** `MentorService.getProgressReport()` now queries `completedLessons` from `TutorSessionRepository.getCompletedSessions()` instead of hardcoded `0`. **Phase 3.3:** Created `lib/features/ingestion/providers/` with `documentExtractorProvider` and `webScraperProvider` Riverpod providers; updated `contentPipelineProvider` to consume them; updated ingestion barrel export. **Phase 3.2:** Changed `StudyProgressTracker.getOverallStats()` `totalStudyTimeHours` from `String` (via `toStringAsFixed(1)`) to raw `num` — updated `OverallStats.model`, `ProgressReport` model, and dashboard/mentor display to format with `formatDecimal` locale-aware helper at presentation layer. **Phase 1.4:** Added `@Deprecated('Use QuestionEvaluation instead')` to `Markscheme` and `@Deprecated('Use EvaluationStep instead')` to `MarkSchemeStep`; added `toJson()`/`fromJson()` to `PlanAdherenceModel`. **Settings:** Removed singleton pattern from `SettingsRepository` (`_instance`/`factory`/`_internal`) — now uses public constructor; updated singleton test to verify new-instance behavior. **Phase 3.4:** Removed `PlanAdherenceTracker` from `InstrumentationService` — `recordPlanAdherence()` now creates `PlanAdherenceModel` records via injected `PlanAdherenceRepository`; updated `dashboardInstrumentationServiceProvider` to inject `dashboardAdherenceRepositoryProvider`; removed 3 `PlanAdherenceTracker` test groups from `instrumentation_service_test.dart` (functionality now covered by `PlanAdherenceRepository` tests). `dart analyze` zero issues on `lib/` and `test/`.
- 2026-05-16: Completed `code_refactor_master.md` (Dashboard: Dead Code, Duplicated Logic, Misplaced Concerns) — **Issue 1:** Deleted dead `services/dashboard_data_loader.dart` (~126 lines) and its test file (zero imports in `lib/`). **Issue 2:** Removed unused intermediate `totalMs` variable from `dashboardFocusStatsProvider` — `totalSeconds` now computed directly via fold. **Issue 3:** Extracted `DashboardLayoutPreferences`, `DashboardLayoutNotifier`, and `dashboardLayoutPreferencesProvider` from `dashboard_data_providers.dart` into new `providers/dashboard_layout_providers.dart`; updated barrel export in `dashboard.dart`; updated imports in `collapsible_card.dart` and 2 test files. `dart analyze` zero new issues; all 279 dashboard tests pass (30 pre-existing widget-test failures unchanged).
- 2026-05-16: Completed `ui_ux_master.md` (UI/UX Master Issue: Accessibility, Animation, Responsive, Design Consistency) — **#1 A11Y:** Added `semanticLabel` to all `AlertDialog` calls (help_dialog, planner_screen, mentor_screen, practice_session_screen); refactored `showQuickGuideHelpDialog` into `QuickGuideHelpDialog` widget. **#2 A11Y:** Replaced `GestureDetector` confidence-selector circles with `InkWell` + `Semantics(button: true)` in `practice_session_screen.dart`; added `Semantics(button: true)` to `PracticeModeCard`. **#3 A11Y:** Replaced all hardcoded `fontSize` below 12sp with theme text styles (`milestone_timeline.dart` 9→labelSmall, `calendar_view_widget.dart` 8→labelSmall, `tutor_screen.dart` 11→labelSmall, `practice_feedback_widget.dart` 18→titleLarge). **#4 RESPONSIVE:** Changed `gridCrossAxisCount` sm breakpoint from 3→2 for even 2+2 grid layout. **#5 ANIMATION:** Added directional `SlideTransition` to `AnimatedSwitcher` with `_previousIndex` tracking for forward/backward cue; respects `reduceMotion`. **#6 VESTIBULAR:** Replaced full-circle 3% pulse scale with subtle breathing ring overlay (15% opacity border) in `focus_timer_widget.dart`; respects both `reduceMotion` and `MediaQuery.disableAnimationsOf`. **#7 RADIUS:** Defined `static const bottomSheetShape` (16px) in `AppTheme`—buttons remain 8, cards 12, sheets/FABs 16. **#8 NAVIGATION:** Added `Tooltip` with l10n (`tapToExpand`/`tapToCollapse`) and `AnimatedSize` smooth collapse/expand to `CollapsibleCard`. **#9 A11Y:** Added `_evaluationSemanticLabel` helper including percentage score in screen-reader label (e.g., "Correct, 85%") in `chat_bubble.dart`. **#10 INPUT:** Added visible `sendHint` hint line below `ConversationInput` ("Press Enter to send, Ctrl+Enter for new line") with l10n support. **#11 LAYOUT:** Removed conditional `if (onPrevious != null)` rendering in `PracticeSessionNavButtons`—Previous button always rendered (disabled via null callback) to prevent layout shift. **#12 BOTTOM SHEET:** Centralized `AppTheme.bottomSheetShape`; updated 3 files (`mistake_review_widget.dart`, `source_practice_sheet.dart`, `practice_mode_sheet.dart`) to use it instead of inline `RoundedRectangleBorder`. Added 3 new ARB keys (`tapToExpand`, `tapToCollapse`, `sendHint`). Updated `conversation_input_test.dart` with `AppLocalizations` delegates. `dart analyze lib/` zero issues; 168 affected tests pass.
- 2026-05-16: Completed `future_functionality_planner.md` (Multi-Format Real Content Ingestion Pipeline) — Created real extraction services in `lib/core/data/extraction/`: `PdfExtractor` (PDF text extraction with regex-based text-object parsing, fallback raw decode, page count detection), `OcrExtractor` (image OCR via LLM fallback, file/URL/base64 input detection), `TranscriptionExtractor` (audio/video transcription via LLM, YouTube transcript fetching via youtubetranscript.com API, video ID extraction for youtube.com/youtu.be URLs). Replaced `DocumentExtractor` stubs: `_extractPdfOrDocument` now uses `PdfExtractor` for real PDF parsing; `_extractImage` delegates to `OcrExtractor` with LLM OCR; `_extractVideo` delegates to `TranscriptionExtractor` with YouTube transcript support; `_extractAudio` delegates to `TranscriptionExtractor` with LLM transcription. Injected `LlmService` into `DocumentExtractor` for OCR/transcription capabilities. Updated `ContentPipeline` to pass `LlmService` to `DocumentExtractor`. Added disposal lifecycle (`DocumentExtractor.dispose()`, `ContentPipeline.dispose()` includes DocumentExtractor). Added 27 new tests across 3 extraction service test files. All 492 tests pass (excluding 7 pre-existing upload_screen Hive initialization failures). `dart analyze` zero issues.
- 2026-05-16: Completed `test_master.md` (Test Coverage & Structural Gaps) — **Issue 1:** Removed SettingsController tests from `app_providers_test.dart` (weaker mock), kept only provider declarations and `defaultModelForProvider`. Deduplicated `settings_controller_test.dart` — removed duplicate Riverpod provider and `defaultModelForProvider` tests that already live in `app_providers_test.dart`. **Issue 2:** Created `test/core/constants/app_constants_test.dart` (11 barrel export tests) and `test/core/constants/llm_defaults_test.dart` (3 `defaultModelForProvider` tests). **Issue 3:** Created `test/core/utils/clock_test.dart` (5 tests: Clock subclassing, SystemClock.now() close to real time, DateTime instance). **Issue 4:** Created `test/core/utils/localization_helpers_test.dart` with content from old location; replaced `test/core/services/localization_service_test.dart` with redirect comment. **Issue 5:** Confirmed `_FakeLessonService` no longer exists in codebase — the `lesson_providers_test.dart` is already clean with 3 simple provider tests. **Issue 6:** Moved app loading widget test to `test/app_test.dart`; deleted orphan `test/core/model_test.dart` (QuestionModel tests already in `question_model_test.dart`). `dart analyze` zero issues.
- 2026-05-16: Completed `code_refactor_master.md` (Hive typeId collisions, misplaced screens, dead param, i18n gaps, barrel over-exposure, time-tracking bug) — **Issue 1 (Hive typeId collisions):** Reassigned `AccessibilityPreferences` from 11→34, `RoadmapModel` from 24→29, `PendingActionModel` from 30→5, `BadgeModel` from 31→8. Added `EngagementNudgeModel` (32) and `PlanAdherenceModel` (33) to tracker. Updated all `.g.dart` adapter files. **Issue 2 (Misplaced screens):** Moved `practice_screen.dart`, `practice_session_screen.dart`, `practice_results_screen.dart` to `presentation/screens/`. Updated all imports (barrel, app_router, main, 5 test files). Added `AppRoutes.examSession` + `ExamSessionArgs`; `practice_screen.dart` now navigates via router instead of direct `Navigator.push`. **Issue 3 (Dead param):** Removed `masteryStateRepo` from `MasteryRecorder` constructor, provider, and all test instantiations (mastery_recorder_test, exam_session_screen_test). **Issue 4 (i18n):** Added 8 ARB keys (`examConfiguration`, `startExam`, `examDuration`, `numberOfQuestions`, `incorrectLabel`, `skippedLabel`, `examAutoSubmitted`, `topicBreakdown`) to both `app_en.arb`/`app_es.arb`; replaced all 8 hardcoded strings in `exam_session_screen.dart`. **Issue 5 (Barrel):** Trimmed `practice.dart` from 33 exports to 7 public-facing exports (screens, services, provider). **Issue 6 (Time tracking):** Added per-question `DateTime` markers in both `exam_session_screen.dart` and `practice_session_screen.dart`; replaced hardcoded `0`/`1` and averaged values with real `timeSpentMs`; fixed `PracticeAnswerRecord.timeSpent` from `Duration.zero` to real duration. `dart analyze` zero issues.
- 2026-05-16: Completed `ui_ux_master.md` (Responsive Layout Fragility, Accessibility Gaps, Fragmented Navigation) — **Issue 1 (Responsive):** Clamped `SubjectDetailScreen` SliverAppBar expandedHeight to 100–200px range. Added `maxBarWidth` (default 48px) to `AnimatedBarChart`; bars centered when container exceeds `count * maxBarWidth + spacing`. Timer font in `SessionTrackerScreen` scales via `clamp(36, 64, shortestSide * 0.09)`. Extra modes row in `PracticeScreen` stacks vertically on `xs` breakpoint via `_ExtraModeCard` + `Column`. **Issue 2 (Accessibility):** Each `AnimatedBarChart` bar wrapped in `Semantics(label: '$day: $count sessions')`. `MetricCard` has per-card `Semantics(label: '$label: $value')`. `Dismissible` in `SessionHistoryScreen` has `Semantics(hint: swipeToDelete)`; background container has `ExcludeSemantics`. `_SessionEndDialog` uses `Form` + `TextFormField` with `validator` enforcing `correct <= questions` and `>= 0`. Added 6 new ARB keys (`valueMustBePositive`, `correctExceedsQuestions`, `backToPractice`, `startingPractice`, `swipeToDelete`). **Issue 3 (Navigation):** Created shared `PracticeSheetTemplate` widget; `TopicSelectionSheet` migrated to use it. `SpacedRepetitionSheet.showAllCaughtUp` includes "Back to Practice" button. FAB flow shows brief "Starting practice…" dialog. Export menu in `SessionHistoryScreen` grouped into Quick/Comprehensive sections with section headers. Duplicate `tune` icon removed from `PracticeScreen` app bar. **Testing:** Added semantics-label tests for `AnimatedBarChart`, `MetricCard`, `SessionHistoryScreen` Dismissible hint, and `PracticeScreen` responsive xs stacking. Fixed `subjectRepositoryProvider` override in `practice_screen_test.dart` (pre-existing provider mismatch). All 62 core/animated/metric/history tests pass.
- 2026-05-16: Completed `future_functionality_planner.md` (Mentor-Planner Convergence & Proactive Engagement Engine) — **Phase 1:** Expanded `MentorService._buildContextPrompt()` to inject plan status (day/progress), roadmap milestones, pending actions, upcoming lessons (next 3), weak topics (accuracy %), wellbeing signals (daily minutes vs cap, late-night warnings, consecutive study days). **Phase 2:** Replaced `_checkAndHandlePlanningIntent()` naive PendingActionModel passthrough with direct conversational scheduling via `PlannerService.scheduleLesson()` + `hasSchedulingConflict()` + `_findNextFreeSlot()`. Schedule/reschedule keywords now propose time slots, check conflicts, and book directly. Plan/roadmap keywords trigger conversational confirmation flow. **Phase 3:** Added `checkWellbeingAndGenerateNudges()` — generates overwork nudges (daily time > cap), late-night session detection, revision nudges (at-risk questions), streak celebrations (7+ days), inactivity check-ins (48h+). All nudges persisted via `EngagementNudgeRepository` and surfaced to mentor conversation memory. **Phase 4:** Wellbeing monitor tracks daily study minutes, consecutive study days, late-night sessions. **Phase 5:** Accountability/motivation — streak congratulations at 7 days, low-activity check-ins at 48h. **Phase 6 Cleanup:** Removed `AdaptivePracticeEngine` (dead, superseded by `SpacedRepetitionEngine`); removed `lessonPlanProvider` + `promptTemplatesProvider` dead providers from `teaching_providers.dart`; removed 6 unused `FutureProvider.family` from `lesson_providers.dart`; removed dual block storage (`_blockBox`/`addBlock`/`getBlocksForLesson`/`getBlocksBySubject`) from `LessonRepository` + `hive_initializer.dart` + `database_migration.dart`; consolidated `PlannerService.redistributeWorkload()` and `linkDailyPlanToRoadmap()` to delegate to `PersonalLearningPlanService` (new public `redistributeMissedWorkloadForStudent()` method). New dependencies: `PlannerService` + `EngagementNudgeRepository` + `SessionRepository` injected into `MentorService` + `mentor_providers.dart` + `mentor_screen.dart`. `dart analyze` zero issues; 143 directly affected tests pass (39 mentor service, 8 engagement scheduler, 7 teaching providers, 3 lesson providers, 24 planner service, 62 planner others). New test coverage for conversational scheduling, context expansion, and engagement nudge generation.
- 2026-05-16: Completed `internationalisation_master.md` (Systematic Locale-Unaware Percentage Formatting & Hardcoded User-Facing Strings) — **Issue 1:** Verified all 12 `formatPercent()` replacements already applied across 8 files (`progress_overlay_widget`, `plan_summary_card`, `lesson_progress_bar`, `chat_bubble`, `weak_areas_card`, `topic_breakdown_card`, `mastery_progress_card`, `plan_adherence_card`). **Issue 2:** Replaced raw `roadmap.status` text in `roadmap_card.dart:51` with `l10n.inProgress`/`l10n.completed`/`l10n.notStarted` via `switch` expression. **Issue 3:** Replaced hardcoded `'M${milestone.order}'` in `milestone_timeline.dart:86` with `l10n.milestoneShort(milestone.order)` — added `milestoneShort` ARB key (EN: `M{order}`, ES: `H{order}`). **Issue 4:** Replaced `'${totalQuestions}Q'` in `plan_summary_card.dart` with `l10n.questionsAbbreviation()` — added `questionsAbbreviation` ARB key (EN: `{count}Q`, ES: `{count}P`); replaced `'${targetMinutes}m'` in `calendar_view_widget.dart` with `l10n.minutesCountMetric()`; replaced `'${durationMinutes}min'` in `lesson_progress_bar.dart` with `l10n.durationMinutes()`. **Issue 5:** Replaced hardcoded ISO `YYYY-MM-DD` in `lesson_booking_sheet.dart:106` with `DateFormat.yMd(l10n.localeName)`. Added Spanish-locale widget test in `plan_summary_card_test.dart` verifying `'100P'` question abbreviation. Updated 4 existing test files for new localized values. `flutter gen-l10n` regenerated; `flutter analyze` zero issues; all 64+ affected tests pass.
- 2026-05-16: Consolidated quickguide screen test suite — 4 redundant test files (`quick_guide_screen_advanced_test.dart`, `quick_guide_screen_coverage_test.dart`, `quick_guide_screen_extra_test.dart`) merged into single `quick_guide_screen_test.dart` (61 tests). Extracted shared `FakeLlmService`, `TestNavigatorObserver`, `buildTestApp`, and `buildTestAppWithProvider` into `shared_screen_test_helpers.dart`. Eliminated 3× code duplication of fake/service/helper classes. Each source file in `lib/features/quickguide/presentation/` now has exactly one corresponding test file.
- 2026-05-16: Completed `test_master.md` (Core Error Module Structural Redesign) — **AC1:** Split `exceptions_test.dart`: unit tests stay, widget tests moved to new `handlers_widget_test.dart`. **AC2:** Extracted `captureContext` helper to `test/core/errors/shared_test_helpers.dart`; all 3 error test files now import from shared. **AC3:** Clear responsibility boundaries: `exceptions_test.dart` → exceptions.dart (unit), `handlers_test.dart` → handlers.dart (unit), `handlers_widget_test.dart` → handlers.dart (widget), `result_and_conversion_test.dart` → result.dart (unit). **AC4:** Refactored `_convertToAppException` to `@visibleForTesting convertToAppException` in `handlers.dart`; added 22 direct synchronous unit tests covering all 18 conversion rules plus priority ordering and edge cases. **AC5:** Made `AppErrorHandler.logger` injectable; added 4 spy-based tests verifying `_logError` fires with correct tag and message for `handleError`, `handleSyncError`, `safely`, `safelySync`. **AC6:** Created `test/features/teaching/services/conversation_phase_test.dart` with 4 tests covering all 6 enum values, correct ordering, index verification, and name uniqueness. `dart analyze` zero issues; all error-module tests pass.
- 2026-05-16: Completed `code_refactor_master.md` remaining items (Duplicated Error Handling, Barrel Coverage, Hardcoded Model Defaults) — Unified `handleSyncError` in `lib/core/errors/handlers.dart` to delegate to `handleError`, eliminating ~80% duplicated logic. Added `localization_helpers.dart` export to `lib/core/utils/utils.dart` barrel. Created `lib/core/constants/llm_defaults.dart` with relocated `defaultModelForProvider()`; re-exported via `app_constants.dart`; updated all 7 import sites (3 lib + 4 test) to import from the constants barrel. Confirmed AC2 (formatCurrency has callers, bottomSheetShape already merged into `app_runtime_config.dart`), AC3 (localization_service.dart removed), AC5 (locale_config.dart no longer has `buildDropdownItems`), AC6 (llm_task_status.dart already extracted), and AC7 (all 7 `toStringAsFixed()` calls are CSV/LLM-facing, not user-facing) were already satisfied by prior work. `dart analyze` zero issues; no new test failures (all pre-existing 26–40 failures unchanged).
- 2026-05-16: Completed `ui_ux_master.md` remaining items (Accessibility, Animation, Locale-Formatting) — **Problem 1 (Semantic Accessibility):** Added `Semantics(liveRegion: true)` to streaming text in `chat_bubble.dart` (tutor/mentor replies), timer display in `session_tracker_screen.dart`/`focus_timer_widget.dart` (elapsed/countdown), and `practice_feedback_widget.dart` (correct/incorrect announcements). Added `Semantics(button: true)` to `practice_mode_option.dart` (trailing arrow grouped), `topic_list_screen.dart` (chevron+onTap). Added `enabled:` to `practice_mode_card.dart` for disabled state. Added semantic labels to `practice_session_question_card.dart` TextFields. Removed double-`Semantics(button: true)` wrapping from `session_history_screen.dart` PopupMenuButton and `question_card_widget.dart` Submit/Next buttons. Wrapped `session_analytics.dart` metric cards in semantic group. **Problem 2 (Reduced Motion):** Added `reduceMotion` parameter to `FocusTimerWidget`/`VoiceBar` (pass from `settingsProvider`). Gated pulse animation in `focus_timer_widget.dart` with both `reduceMotion` and `MediaQuery.disableAnimationsOf`. Gated voice-bar `_waveController.repeat()` by `reduceMotion`. Replaced `animateTo` (100ms) with `jumpTo` in `tutor_screen.dart`, `mentor_screen.dart`, `quick_guide_screen.dart` when `reduceMotion=true`. Fixed `animated_bar_chart.dart` `_hasAnimated` flag (added `onEnd` callback) so initial animation only runs once. Added `AnimatedSwitcher` fade transition for question changes in `practice_session_screen.dart` (gated by `reduceMotion`). Removed empty `setState(() {})` from `session_history_screen.dart`. **Problem 3a (Hardcoded Colors):** Replaced `Colors.green`/`Colors.red`/`Colors.orange` in `chat_bubble.dart` with `colorScheme.primary`/`error`/`tertiary`. Replaced `Colors.blue` in `topic_list_screen.dart` with `colorScheme.primary`. Replaced hardcoded `Colors.*` in `practice_session_screen.dart` confidence selector with `colorScheme`. **Problem 3b (Locale Formatting):** Fixed 12 files using `'${(value * 100).round()}%'` pattern — replaced with `formatPercent(value * 100, l10n.localeName, ...)` in `weak_areas_card.dart`, `topic_breakdown_card.dart`, `mastery_progress_card.dart`, `plan_adherence_card.dart`, `lesson_progress_bar.dart`, `chat_bubble.dart`, `plan_summary_card.dart`, `progress_overlay_widget.dart` (2 locations). Replaced `_formatDuration` raw `'${hours}h ${minutes}m'` in `session_summary_card.dart` with `formatDurationFromContext`. Added formatPercent for evaluation score in `chat_bubble.dart`. All CSV/LLM strings preserved (en-invariant). `dart analyze` zero issues.

- 2026-05-16: Completed `internationalisation_master.md` (Hardcoded English Strings and Locale-Unaware Formatting) — Added 10 new ARB keys to `app_en.arb`/`app_es.arb` (`filePickerError`, `urlFetchSuccess`, `urlFetchFailed`, `urlFetchError`, `file`, `fetchAndScrape`, `hoursAbbreviation`, `tokensLabel`, `usageRecordFormat`, `usageSummary`). Fixed 6 hardcoded English strings in `upload_screen.dart` (file picker error, URL fetch success/failed/error, File chip label, Fetch & Scrape button). Fixed `summary_row.dart`: replaced `'$accuracy%'` with `formatPercent()`; replaced `'...h'` suffix with `l10n.hoursAbbreviation()`. Fixed `planner_screen.dart` hardcoded `h` suffix with `l10n.hoursAbbreviation()`. Fixed `llm_task_manager_screen.dart`: replaced hardcoded `$` currency sign with `formatCurrency()` in two locations; replaced hardcoded `'...tokens'` label with `l10n.tokensLabel()`. Fixed `settings_model.dart`: replaced hardcoded `$` in `priceDisplay`/`priceDisplayWithLocale` with `formatCurrency()`; added `formattedTextWithL10n()` and `formatUsageSummaryWithL10n()` accepting `AppLocalizations` and using ARB template keys. Fixed `session_export_service.dart`: `_formatTotalDuration`/`_formatDuration` now accept `AppLocalizations` and use `l10n.durationHours()`/`l10n.durationMinutes()`/`l10n.durationSeconds()` instead of hardcoded `'h'`/`'m'`/`'s'`. Kept `lesson_detail_screen.dart` timer `:` separator as non-linguistic digital format (P2, flagged for awareness). `dart analyze` zero issues; i18n coverage check passes (100% ARB key parity); all 77 affected tests pass. Generated l10n via `flutter gen-l10n`.

- 2026-05-16: Completed `code_refactor_master.md` remaining items (Layer Violation Fixes, Dead Code, Log Hygiene, Hardcoded Components, Singleton Anti-patterns, Eager Database, CSV/Calculation Defects) — Removed `updateQuestionState()` / `clearCache()` empty methods from `adaptive_practice_engine.dart`. Fixed 30+ empty `catch (_) {}` blocks in `mentor_service.dart`, `engagement_scheduler.dart`, `study_progress_tracker.dart`, `practice_data_service.dart`, `dashboard_data_loader.dart`, `personal_learning_plan_service.dart` with proper `_logger.e/w` calls. Changed inappropriate `.i()` to `.d()` in `study_timer_service.dart` (5 session lifecycle events), `content_pipeline.dart` (7 pipeline stage events), `instrumentation_service.dart` (export event). Consolidated duplicate `LlmService` system prompts (`'StudyKing'`/`'StudyKing Quick Guide'`) into single `LlmService.defaultSystemPrompt` constant. Centralized hardcoded default LLM URLs (`'http://localhost:11434'`, `'https://api.openai.com/v1'`) into `ApiConfig.ollamaDefaultUrl` / `ApiConfig.openAIDefaultUrl`. Moved hardcoded `MentorService` system prompt to `_mentorSystemPromptText` constant. Converted `StudentIdService` / `NotificationService` from static singleton pattern (`_instance`, factory, `_internal()` constructor) to injectable public `StudentIdService()` / `NotificationService()`. Replaced eager top-level `database` variable in `app_providers.dart` with lazy `databaseProvider` Riverpod `Provider`; updated all consumers (`teaching_providers.dart`, `lesson_providers.dart`, `mentor_screen.dart`, `contentPipelineProvider`, `main.dart`). Fixed CSV malformed export (stray `)` characters on 6 rows) and `_calculateImprovement` operator precedence bug (inconsistent %/ratio units) in `study_progress_tracker.dart`. `dart analyze` zero new issues.
- 2026-05-16: Completed `ui_ux_master.md` (Systemic Hardcoded Color Values Break Dark Mode & Accessibility) — Added `AppTheme.statusColor()` helper mapping `LlmTaskStatus` to `colorScheme` roles. Refactored `MathExpressionWidget._parseExpression` to accept `ColorScheme`, replacing all `Colors.*` with `cs.primary`/`cs.secondary`/`cs.tertiary`/`cs.onSurfaceVariant`. Replaced `Colors.white`/`Colors.red` in `SubjectDetailScreen` with `colorScheme.surface`/`colorScheme.error`. Replaced status chip colors in `LlmTaskManagerScreen` with `AppTheme.statusColor()`. Replaced `Colors.red`/`Colors.green` SnackBar backgrounds in `UploadScreen` and `ApiConfigScreen` with `colorScheme.error`/`colorScheme.primary`. Replaced `Colors.green` chip in `TutorScreen` with `colorScheme.primary`. Replaced hardcoded progress colors in `ProgressOverlayWidget` with `AppTheme.progressColor()`. Set stroke color in `CanvasDrawingWidget._handlePanStart` to `colorScheme.onSurface`. Replaced `Colors.grey` fallback in `MetricCard` with `colorScheme.onSurfaceVariant`. Made `AnimatedBarChart.accentColor` nullable defaulting to `theme.colorScheme.primary`. Updated `math_expression_widget_test.dart` and `progress_overlay_widget_test.dart` color assertions to expect `colorScheme` roles. `dart analyze` zero new issues; no new test failures (all 59 pre-existing failures unchanged).
- 2026-05-16: Completed `future_functionality_planner.md` (AI Tutor Redesign) — Created typed `LessonPlan` model (`lesson_plan_model.dart`) with `LessonSection`, JSON serialization, validation (rejects empty goals, zero-duration sections), and `defaultPlan` fallback. Created `EvaluationResult` model with `score`, `explanation`, `partialCredit`, `conceptBreakdown`. Created `ExerciseEvaluator` service delegating to `LlmService` for semantic evaluation instead of keyword matching; returns structured `EvaluationResult`. Refactored `ConversationPromptSet` replacing `PromptTemplates` — versioned `PromptEntry` (system+user) per phase; added `evaluateExercise` prompt; all prompts extracted from `conversation_manager.dart`. Refactored `ConversationManager`: identity params (`studentId`, `topicTitle`, `subjectId`, `topicId`) now final constructor params; `Clock` injected replacing `DateTime.now()`; `ExerciseEvaluator` injected replacing keyword lists; `generateLessonPlan()` returns typed `LessonPlan`; `_evaluateExerciseResponse()` delegates to `ExerciseEvaluator`; `toSession()` uses `Clock` for `endTime`; `lessonPlan` field stores parsed plan. Refactored `TutorService`: `ExerciseEvaluator`, `Clock`, `QuestionRepository` injected; `startLesson()` properly awaits `manager.initialize()`; `_persistExercisesAsQuestions()` creates `Question` objects from evaluations; all `DateTime.now()` replaced with `_clock.now()`. Created `VoiceController` service with graceful fallback. Created `VoiceBar` widget with mic toggle, waveform animation, transcription display. Updated `TutorScreen`: replaced `StudentIdService()` singleton with `studentIdValueProvider`; replaced `_isVoiceListening` dead flag with `VoiceBar` + `VoiceController`; `startLesson()` no longer passes keyword lists. Updated `LessonProgressBar`: accepts `lessonPlan` parameter renders section timeline with completion/current-section indicators. Updated `ChatBubble`: displays evaluation results (score bar, explanation, concept breakdown). Added `speech_to_text` and `flutter_tts` dependencies. Created 4 new test files (`lesson_plan_model_test.dart`, `evaluation_result_test.dart`, `exercise_evaluator_test.dart`, `voice_controller_test.dart`) and updated 7 existing test files. All 282 teaching tests pass; `dart analyze` zero issues.

- 2026-05-16: Completed `test_master.md` (Test Coverage Gaps & Structural Disorganization) — Category 1 (Missing Coverage): Created `web_scraper_test.dart` (12 tests: URL validation, HTTP 200/4xx/5xx, empty body, script/style stripping, short-line filtering, exception handling, dispose), `document_extractor_test.dart` (13 tests: all 10 SourceType branches in extractText, estimateChunkCount edge cases including empty/exact boundary/partial chunk/single char/custom size), `prompts_test.dart` (24 tests: all 6 ConversationPhase branches, all 3 adaptivePace thresholds, boundary values at 0.8/1.2, interpolation correctness for duration, confidence, exercise counts), `teaching_providers_test.dart` (9 tests: teachingModelIdProvider fallback chain for all 3 providers, tutorServiceProvider creation, promptTemplatesProvider identity). Category 2 (Settings Reorganization): Deleted `model_edge_cases_test.dart` (absorbed SettingsBox type coercion → `settings_box_test.dart`, UserProfile edge cases → `user_profile_model_test.dart`, adapter tests → new `adapters/settings_box_adapter_test.dart`/`user_profile_adapter_test.dart`). Created `llm_models_edge_cases_test.dart` (15 tests: DynamicModel.getBestPrice with empty/single/multiple/same-total prices, calculateCost with zero tokens/empty prices, OpenRouterRequest.toJson temperature/maxTokens/topP defaults/omissions). Moved `provider_wiring_test.dart` tests into `test/core/providers/llm_providers_test.dart` (added 4 wiring tests + defaultModelForProvider defaults test), deleted old file. Merged `settings_repository_hive_test.dart` into `settings_repository_test.dart` (added 6 edge case groups: fallback profile scan paths, settings persistence, clear idempotency, box independence, API key edge cases, null updateStats/updateSettings preservation). Category 3 (Provider Expansions): Expanded `topic_repository_provider_test.dart` (added override test with fake reads, error propagation test). Expanded `dashboard_providers_test.dart` (added 3 override propagation tests for TopicRepository, AttemptRepository, AdherenceRepository). Expanded `practice_providers_test.dart` (added 3 dependency wiring tests verifying practiceDataServiceProvider depends on spacedRepetitionServiceProvider, sessionRepositoryProvider, subjectRepositoryProvider). Edge Cases: Expanded `progress_repository_test.dart` (added 7 edge case tests: zero-time recordAttempt, negative timeSpentMs, averageTimeMs on 1st/2nd/3rd sequential attempts, concurrent Future.wait isolation across same/different topics). All new tests pass; `dart analyze` zero issues.
- 2026-05-16: Completed `internationalisation_master.md` (Locale-Unaware Number Formatting) — Created `lib/core/utils/number_format_utils.dart` with 5 locale-aware helpers (`formatDecimal`, `formatPercent`, `formatCompactNumber`, `formatHours`, `formatCurrency`). Replaced `toStringAsFixed(n)` with locale-aware formatting in all user-facing displays: subject stats, practice results, practice stats bar, LLM task manager costs/tokens, notification/engagement-scheduler hour strings, dashboard hours display (FocusTodayStats returns raw double, formatting delegated to `formatHours` at presentation layer), settings model cost/token display, PDF export date format (`DateFormat(‘yyyy-MM-dd HH:mm’)` → `DateFormat.yMd(l10n.localeName).add_Hm()`), PDF mastery table percentages. Moved `FocusTodayStats.hours` from `String` (with `toStringAsFixed`) to `double` getter, removed duplicate `hours` field from dashboard data provider/loader maps (formatting now at screen layer). Fixed `app_es.arb:2299` `"mates"` → `"matemáticas"`. Documented i18n formatting conventions in `AGENTS.md`. Added 13-unit test file `test/core/utils/number_format_utils_test.dart` verifying `es` locale produces comma decimal separators for all 5 helpers. Fixed 3 secondary analysis warnings (unnecessary null-aware operator, 2 unused imports, unnecessary string interpolation). `dart analyze` zero issues; all 13 number-format tests pass.

- 2026-05-16: Completed `code_refactor_master.md` (Practice Feature: Immutable Models, DI Fixes, Clock Abstraction, Repository Rename, Provider Tests) — Issue A: Made `QuestionMasteryState` immutable — all 15 fields now `final`, `recordAttempt()` returns new instance instead of mutating, accepts `DateTime now` parameter, helper methods (`_updateMasteryLevel`, `_updateReviewUrgency`, `_calculateNextReview`, `_recencyScore`) become static pure functions. Issue B: Fixed DI bypasses — added `subjectRepositoryProvider`, `attemptRepositoryProvider`, `masteryStateRepositoryProvider`, `questionMasteryStateRepositoryProvider`, `topicDependencyRepositoryProvider`, `questionEvaluationRepositoryProvider` to `practice_providers.dart`; replaced all `SubjectRepository()` and `StudentIdService().getStudentId()` direct calls with injected dependencies in `PracticeScreen`, `PracticeSessionScreen`, `PracticeDataService`, `PracticeSessionService`; removed `param ?? ConcreteClass()` defaults from `SpacedRepetitionService`, `SpacedRepetitionRepository`, `MasteryGraphRepository`, `MasteryGraphService` (production constructors); fixed `MasteryGraphRepository.test()` to accept pre-configured sub-repositories. Issue C: Created `Clock` abstraction (`lib/core/utils/clock.dart`) with `SystemClock`; injected into `PracticeSessionService` replacing `DateTime.now()` calls; replaced `startTimer(BuildContext)` with `startTimer()` (no context), exposed `ValueNotifier<Duration> elapsedNotifier` as single source of truth; removed screen `_displayTimer` in favor of listening to service's notifier; removed `elapsedTimeFormatted`/`timer` getters from service; `BuildContext` removed from service layer. Issue D: Renamed `answer_repository.dart` → `question_choice_repository.dart` to match class name. Issue E: Rewrote `practice_providers_test.dart` with `ProviderScope`/`overrides` and override-coverage tests. `dart analyze` zero issues.

- 2026-05-16: Continued `ui_ux_master.md` (Broken Accessibility: Duplicate NumericFocusOrder Values) — Removed `headingLevel: 2` from `CollapsibleCard` button semantics to fix screen reader conflict. Fixed `practice_session_screen.dart` — renumbered `NumericFocusOrder(4)` → `NumericFocusOrder(3)` for sequential focus ordering. Fixed `quick_guide_screen.dart` — replaced `AnimatedOpacity`-hidden `SuggestedPromptsWidget` with conditional rendering so invisible chips are not reachable by keyboard/screen reader. Added 30+ keyboard accessibility widget tests across all 9 affected screens (lesson_list, lesson_detail, session_tracker, session_history, settings, dashboard, planner, quickguide, practice_session) verifying `FocusTraversalGroup` presence, interactive element reachability, and keyboard alternatives for touch-only interactions. `dart analyze` zero issues; all tests pass (2 pre-existing planner milestone-count failures unchanged).
- 2026-05-16: Completed `test_master.md` (Test Coverage & Quality: `lib/core/` Untested Files, Shallow Coverage) — Verified all 19 `lib/core/` source files from the issue now have substantive tests (AC1 met). Verified `mastery_graph_service_test.dart` and `mastery_integration_service_test.dart` already contain value assertion groups and error propagation tests (AC2 met). Verified `pdf_ingestion_service_test.dart` already covers all 4 methods (parsePdf, classifyTopic, extractQuestions, generateSummary) beyond the API-key guard (AC3 met). Confirmed orphaned `evaluation_adapter_service_test.dart` no longer exists (AC4 met). Confirmed `test/core/routes/main_screen_test.dart` no longer exists (already moved to `test/main_screen_test.dart`, AC5 met). Confirmed only 3 error-handler test files remain (consolidated from 6, AC6 met). Confirmed `dashboard_barrel_test.dart` does not exist (renamed to `dashboard_test.dart`, AC7 met). Deepened `llm_providers_test.dart` from 2→10 tests with provider wiring/override/isolation coverage. Deepened `hive_type_ids_test.dart` with additional assertions on `validateHiveTypeIds`. Deepened `repository_test.dart` from 3→12 tests with Hive-backed CRUD operations (save, get, getAll, delete, overwrite, filterBy, box accessor). All 24 new/deepened tests pass; `dart analyze` zero issues on all changed files.
- 2026-05-16: Completed `future_functionality_planner.md` (Content Ingestion & Processing Pipeline) — Added `ProcessingStatus` enum (`pending`, `extracting`, `classifying`, `generatingQuestions`, `completed`, `failed`) and `image`, `webPage`, `audio`, `document` SourceType values to `enums.dart`. Extended `Source` model with `processingStatus`, `extractedText`, `generatedQuestionIds` fields + `statusEnum` getter + updated `toJson`/`fromJson`/`copyWith`. Added `getByStatus`/`getPending`/`getFailed`/`getCompleted` to `SourceRepository`. Created `DocumentExtractor` service for format-agnostic text extraction. Created `WebScraper` service with HTML tag stripping for URL content fetching. Refactored `ContentPipeline` to use `LlmService` through DI (replacing `PdfIngestionService`), implementing 4-stage pipeline: text extraction → topic classification → summary generation → optional question generation with `ProcessingStatus` progression tracking and error recovery. Refactored `PdfIngestionService` to delegate all LLM calls to `LlmService.chat()` removing direct `http.post`/hardcoded `openRouterBaseUrl`. Updated `EmbeddingService` to be provider-agnostic using `LlmProvider` config instead of hardcoded OpenRouter URL. Extended `UploadScreen` with native file picker (PDF/txt/md/images/docx/epub), camera capture for images, URL fetch-and-scrape button, file type inference, and processing status display. Created `IdGenerator` utility and `contentPipelineProvider` in `app_providers.dart`. Added `file_picker` dependency. Updated barrel exports. All 72 non-pre-existing ingestion tests pass; `dart analyze` zero issues.
- 2026-05-16: Completed `internationalisation_master.md` (Spanish Localisation Audit & Architecture Improvements) — Issue 1: Consolidated 79 groups of duplicate translation keys in both `app_en.arb`/`app_es.arb` (removed `teachingMode`, `planExplanation*`, `adherenceLow[73]Days`, `notification*` duplicates, `recommendation*`, `suggestion*`); updated all Dart references in `localization_service.dart`, `notification_service.dart`, `study_progress_tracker.dart`, `adaptive_practice_engine.dart`, `test/l10n/app_localizations_final_coverage_test.dart` to use consolidated "Keep" keys; renamed 6 channel desc keys (`notificationChannel*Desc` → `notifChannel*Desc`) for consistency. Issue 2: Verified `adherenceLowToday`/`adherencePartialToday`/`adherenceExceededToday` placeholders already have `"type": "int"` in both ARB files. Issue 3: `main.dart` already uses `AppLocalizations.of(context)?.appTitle ?? 'StudyKing'`. Issue 4: `scripts/check_i18n_coverage.sh` validates 100% key parity; `scripts/validate_arb_no_duplicates.dart` detects duplicate ARB keys. `flutter gen-l10n` zero warnings; `dart analyze lib/ test/` zero issues; all 566 l10n tests pass.
- 2026-05-16: Completed `code_refactor_master.md` (Teaching Feature: Presentation-Layer Service Construction, Hardcoded Configuration, and Missing Provider Abstraction) — AC1: Created `lib/features/teaching/providers/teaching_providers.dart` with `teachingModelIdProvider`, `tutorServiceProvider`, and `promptTemplatesProvider` Riverpod providers. AC2/AC3: Removed inline `MasteryGraphService()` construction and hardcoded model ID from `tutor_screen.dart`; `TutorService` now obtained via `tutorServiceProvider` which reads `masteryGraphServiceProvider` and `teachingModelIdProvider` (from settings). AC4: Extracted 3 LLM system prompts (lesson plan, tutor, summary) from `conversation_manager.dart` into `lib/features/teaching/services/prompts/prompts.dart` via injectable `PromptTemplates` class with default `PromptTemplates.defaultTemplates`. AC5: Refactored `ConversationMemory.messages` from `List<Map<String, String>>` to `List<ConversationMessage>`; `getHistory()` now returns typed objects; eliminated manual map reconstruction in `ConversationManager.messages` getter; updated `LlmService` to convert typed messages via `ConversationMemory.fromConversationMessages`. AC6: Added `await` to `manager.initialize()` in `tutor_service.dart` to eliminate race condition. Extracted `ConversationPhase` enum to dedicated `conversation_phase.dart` to avoid circular imports. All 233 teaching feature tests pass; zero analysis issues.
- 2026-05-16: Completed `ui_ux_master.md` (Accessibility, Theming, Responsive Navigation & i18n) — Issue 1: Replaced `TextScaler.noScaling` with compounded OS text scale × in-app font size factor clamped to [1.0, 2.0] in `main.dart` (fixes WCAG 1.4.4 violation). Issue 2: Replaced hardcoded `Colors.orange` with `colorScheme.tertiary` in `lesson_progress_bar.dart` and hardcoded `Colors.red` with `colorScheme.error` in `tutor_screen.dart`. Issue 3: Added responsive `NavigationRail` for tablet breakpoints (≥840px) in `MainScreen` with FAB moved into `NavigationRail.leading`; added `navigationRailTheme` to `AppTheme._baseTheme`. Issue 4 (already fixed): `calendar` ARB key exists and `planner_screen.dart` uses `l10n.calendar`. `flutter analyze` zero issues, all 233 l10n/core tests pass.
- 2026-05-16: Completed `test_master.md` (Test File Placement & Coverage Structural Fixes) — Verified all 4 issues resolved: Issue 1 (lesson model tests at `test/features/lessons/data/models/` instead of `test/core/data/models/`), Issue 2 (all 15 missing model tests across planner/questions/teaching/subjects/ingestion exist with constructor, toJson, fromJson, copyWith, null edge case, equality/hashCode coverage — 236 passing tests), Issue 3 (barrel tests for all 10 features exist), Issue 4 (stale `test/features/subjects/models/subject_model_test.dart` already removed); Fixed 2 compilation errors in `conversation_message_model_test.dart` and `tutor_session_model_test.dart` where `equality` groups were outside parent group referencing out-of-scope variables; `flutter analyze` zero issues; all 236 model tests pass
- 2026-05-15: Completed `future_functionality_planner.md` (Configurable Runtime AI Provider Switching System) — Added `llmProviderProvider` StateProvider and `defaultModelForProvider` helper to `app_providers.dart`; fixed `llmServiceProvider` in `llm_providers.dart` to read `apiBaseUrlProvider`, `llmProviderProvider`, and `selectedModelProvider` instead of hardcoding `LlmProvider.openRouter`; added `saveProvider`/`getProvider` methods to `SettingsRepository` for Hive persistence of provider selection; added provider selection dropdown (OpenRouter/Ollama/OpenAI) to `api_config_screen.dart` with auto-fill of default Ollama URL; replaced hardcoded `'openai/gpt-4o-mini'` model ID in `tutor_screen.dart` with `ref.watch(selectedModelProvider)` + `defaultModelForProvider` fallback; removed remaining hardcoded `'openai/gpt-4o-mini'` defaultModelId in `quick_guide_screen.dart` (now uses per-provider fallback via `defaultModelForProvider`); added `defaultModelForProvider` fallback to `mentorModelIdProvider` in `mentor_providers.dart`; added provider wiring integration test (`test/features/settings/presentation/provider_wiring_test.dart` — 6 tests verifying llmServiceProvider reflects provider/URL/API key changes); per-provider defaults: `gemini-2.0-flash` (OpenRouter), `llama3` (Ollama), `gpt-4o-mini` (OpenAI)
- 2026-05-15: Completed `internationalisation_master.md` (Planner Feature L10n Bleed: Hardcoded English Strings Bypass Translation System) — Added 24 ARB keys to both `app_en.arb`/`app_es.arb` (`planGeneratedSuccessfully`, `syllabusPlanGenerated`, `failedToGenerateSyllabusPlan`, `failedToCreateRoadmap`, `failedToUpdateMilestone`, `actionAccepted`, `failedToExecuteAction`, `failedToAcceptAction`, `failedToDismissAction`, `lessonScheduled`, `failedToScheduleLesson`, `planRegeneratedFromAdherence`, `failedToRegeneratePlan`, `missedWorkloadRedistributed`, `failedToRedistributeWorkload`, `progressOverview`, `todaysProgress`, `weekly`, `actual`, `planned`, `noStudyPlanYet`, `calendar`, `redistribute`, `topicCount`, `syllabusTopics`, `masteryRequirement`, `noTopicsFoundForSubject`, `failedToResolveSyllabus`, `failedToGetQuestionsForTopic`, `failedToGetQuestionsForTopics`); refactored `PlannerNotifier` — all 14 methods now accept `AppLocalizations l10n` parameter (consistent pattern); replaced 21 hardcoded success/error messages in `planner_providers.dart` with ARB lookups; replaced all hardcoded strings in `progress_overlay_widget.dart` (10 strings) with `l10n.*` calls; replaced hardcoded day abbreviations `['M','T','W','T','F','S','S']` in `calendar_view_widget.dart` and `progress_overlay_widget.dart` with `DateFormat.E(l10n.localeName)`; passed `l10n.localeName` to all `DateFormat.xxx()` calls in `milestone_timeline.dart` (yMMMd, MMMd), `roadmap_card.dart` (yMMMd), `calendar_view_widget.dart` (yMMM, E), `planner_screen.dart` (Hm); removed `.toLowerCase()` hacks from `roadmap_card.dart` and `planner_screen.dart`; replaced hardcoded `'${n} topics'` with `l10n.topicCount(n)` ICU plural in `roadmap_card.dart`; replaced hardcoded `'Calendar'`/`'Redistribute'`/`'No study plan yet'` tab/button/empty-state in `planner_screen.dart` with `l10n.calendar`/`l10n.redistribute`/`l10n.noStudyPlanYet`; replaced manual `'${hour}:${minute}'` formatting with `DateFormat.Hm(l10n.localeName)` in `planner_screen.dart`; replaced hardcoded `'Topics: ...'` and `'Mastery >= 80%'` strings in `planner_service.dart` with `l10n.syllabusTopics()`/`l10n.masteryRequirement`; added `AppLocalizations? l10n` parameter to `SyllabusResolver.resolveSyllabus()`, `getQuestionsForTopic()`, `getQuestionsForTopics()` with localized error fallbacks; removed `AppLocalizations?` nullable pattern from `scheduleLessonWithConflictCheck` (now required); verified no remaining hardcoded planner English strings (grep-confirmed all 10 ACs); regenerated l10n; `dart analyze` zero errors
- 2026-05-15: Completed `code_refactor_master.md` (Inconsistent Code Quality Patterns) — Removed duplicate `session_export_test.dart` (merged unique coverage into canonical `session_export_service_test.dart`: quote/newline CSV escaping, duration rounding, planned duration CSV test, JSON `toJson()` match test, all PDF tests, CSV/JSON/PDF file content matching tests); removed empty `init()` method from `SessionRepository` and all call sites (`session_tracker_screen.dart`, `session_history_screen.dart`, `database_service.dart`); deleted empty `lib/features/focus_mode/data/` directory; normalized imports in `session_tracker_screen.dart` and `session_history_screen.dart` to `package:studyking/...` style; changed all 20 `_logger.e()` calls in `session_repository.dart` to `_logger.w()` (all recoverable operational failures); updated `Logger.w()` signature to accept optional `error`/`stack` parameters for consistency with `Logger.e()`; removed `init()` overrides from all `FakeSessionRepository` subclasses across 8 test files; removed stale `init`-related test assertions
- 2026-05-15: Completed `test_master.md` (Test Coverage Quality & Structural Gaps) — Issue 1: Added `expect` assertions to 2 vacuous tests in `plan_summary_card_test.dart` (coverage percentage text, focus areas absent). Issue 2: Removed duplicate identical test (`shows title and form labels`) from `planner_screen_test.dart`. Issue 3a: Added tap/decrease/assert interaction to "duration can be decreased" test in `lesson_booking_sheet_test.dart`. Issue 3b: Added `Text.style.color` and `LinearProgressIndicator.valueColor` verification to all 3 `progress_overlay_widget_test.dart` color tests. Issue 3c: Added 4 edge-case tests to `calendar_view_widget_test.dart` (empty plan, rest day without minutes, no-topic tap does not fire callback, month-boundary Dec→Jan navigation). Issue 3d: Added 3 conflict-UI tests to `lesson_booking_sheet_test.dart` (conflict warning icon rendering, conflict-blocked scheduling shows SnackBar, no conflict warning when plannerService is null). Issue 3e: Added 4 Pending Actions tests to `planner_screen_test.dart` (section visibility with actions, accept action, dismiss action, hidden when empty). Issue 4: Created `test/helpers/fakes.dart` with 8 canonical shared fake classes (FakePlanRepository, FakeMasteryGraphRepository, FakeTopicRepository, FakeRoadmapRepository, FakeTutorSessionRepository, FakePendingActionRepository, FakePlanAdapter, FakeSessionRepository, FakeQuestionRepository) available for cross-file reuse. Fixed Hive initialization in `lesson_booking_sheet_test.dart` by adding `setUpAll` block, added `AppLocalizations` delegates to all `MaterialApp` wrappers. All 33 widget tests + 42 planner screen tests pass (5 pre-existing plan-generation failures unchanged). — Issue 1: Replaced `AnimationController`-based break timer with `Timer.periodic` in `focus_timer_screen.dart` to eliminate listener accumulation bug where each session completion added a new `addListener` closure (break time shrunk proportionally per cycle). Issue 2: Fixed `_syncPulseAnimation` in `focus_timer_widget.dart` to properly stop/reset the pulse controller when paused and restart when resumed; wrapped timer circle in `RepaintBoundary` to isolate rendering. Issue 3: Replaced Dashboard tab with Focus Mode tab in `NavigationBar` (index 3) in `main.dart`; changed FAB from tab-switching to pushing Dashboard route on current navigator; added `FocusTimerScreen` import. Fixed `SessionSummaryCard` key mismatch (`totalMs`/`totalSeconds` fallback) in `session_summary_card.dart`. Updated `main_screen_test.dart` for new tab structure. `dart analyze` zero errors, all 82 focus mode + main screen tests pass.
- 2026-05-15: Completed `future_functionality_planner.md` (Data Model Consolidation & Architectural Debt Resolution) — Issue 1: Added `autoRegeneration` to canonical `NudgeType` enum in `engagement_nudge_model.dart`, removed duplicate enum from `engagement_scheduler.dart`. Issue 2: Renamed `Answer` model to `QuestionChoice` and `AnswerRepository` to `QuestionChoiceRepository` to eliminate confusion with `StudentAttempt`. Issue 4: Split `MasteryGraphRepository` god class into 4 individual repositories (`MasteryStateRepository`, `QuestionMasteryStateRepository`, `TopicDependencyRepository`, `QuestionEvaluationRepository`); refactored `MasteryGraphRepository` into facade; updated `MasteryGraphService` to use individual repos directly. Issue 5: Created `SpacedRepetitionService` depending on `QuestionRepository` instead of extending `Repository<Question>`; refactored `SpacedRepetitionRepository` as delegation wrapper; moved static `SpacedRepetitionQueries` to service file. Issue 8: Extracted shared `_buildPlan` method in `PersonalLearningPlanService` eliminating ~200 lines of duplicated code between `generatePlan()` and `generatePlanFromSyllabus()`. Issue 9: Removed `WidgetRef` dependency from `PracticeDataService`; constructor takes concrete `SpacedRepetitionService`, `QuestionRepository`, `SubjectRepository`; updated `practice_screen.dart` and provider. Created 5 new files: `mastery_state_repository.dart`, `question_mastery_state_repository.dart`, `topic_dependency_repository.dart`, `question_evaluation_repository.dart`, `spaced_repetition_service.dart`. Updated barrel exports. Updated test files for renamed/new APIs.
- 2026-05-15: Completed `code_refactor_master.md` (Architectural Inconsistency: Scattered Hive Adapters & Unclear Model Ownership) — Fixed broken `question_model.dart` import (markscheme_model moved to questions feature); moved 14 single-feature models from `core/data/models/` to their respective features (`practice`, `planner`, `lessons`, `dashboard`, `settings`, `ingestion`, `subjects`); kept 4 shared models in core with doc comments (`session_model`, `subject_model`, `topic_model`, `question_model`); created canonical `sessionRepositoryProvider` in `sessions/providers/` eliminating duplicate declaration in `practice_providers.dart` and `focus_mode_providers.dart`; simplified `hive_initializer.dart` using feature barrel files (`questions_data.dart`, `practice_data.dart`, `planner_data.dart`, `subjects_data.dart`, `teaching_data.dart`); removed `hide sessionRepositoryProvider` from `features.dart`; added doc comments to 4 shared core models justifying cross-feature ownership; updated all imports across `lib/` and `test/` (200+ files); `flutter analyze` zero new errors (remaining 98 issues are pre-existing test fake overrides unrelated to this change)
- 2026-05-15: Completed `test_master.md` (Critical Test Gaps: 8 Untested Core Models, 6 Missing Feature Tests, Pervasive Inadequate Coverage) — Verifying all 8 core data model tests exist (badge, engagement_nudge, mastery_improvement_metric, pending_action, plan_adherence_metric, plan_adherence, roadmap, student_availability); all 6 feature-level test files exist with substantive coverage (student_availability_repository, calendar_view_widget, progress_overlay_widget, action_executor, session_export_service, topic_repository_provider); all 28 model tests cover ==/hashCode, toString; 20/28 cover fromJson error handling; database_service_test/student_id_service_test/plan_repository_test test real logic (not mocks/placeholders); hive_initializer_test verifies all 20 boxes and 11 adapter IDs; source_model_test covers toJson/fromJson/copyWith/==/hashCode/toString/edge cases; added barrel export test for questions feature (8 export assertions); all 550+ model tests + 372 questions tests + 74 service/repository/initializer tests pass Added `copyWith` tests for `PersonalLearningPlan` and `DailyPlan` in `personal_learning_plan_model_test.dart` (9 fields each, null semantics, immutability); added 3 `fromJson` edge case tests to `source_model_test.dart` (null id, missing id, empty JSON map); verified all 28 model tests now cover `==`/`hashCode`, `toString`, and fromJson error handling where applicable; confirmed all 20 model tests for models with `copyWith` in source now test it; verified all 6 feature-level test files exist with adequate coverage; verified `hive_initializer_test.dart` checks all 20 boxes and all 11 adapter type IDs; verified `database_service_test.dart`, `student_id_service_test.dart`, `plan_repository_test.dart` test real logic (not mocks/placeholders); all 900+ tests pass
- 2026-05-15: Completed `future_functionality_planner.md` (Session Model Unification) — Added missing `focusSessions` constant to `HiveBoxNames` fixing runtime crash in `SessionMigrationService`; deleted 5 stale test files referencing removed `StudySession`/`FocusSession` models; rewrote `session_export_test.dart` to use unified `Session` model instead of `StudySession`; rewrote `subject_detail_screen_test.dart` to use `SessionRepository`/`Session` instead of `StudySessionRepository`/`StudySession`; rewrote `focus_timer_screen_test.dart` to use `StudyTimerServiceProvider`/`StudyTimerService` with `Session` model instead of `FocusSessionService`; removed empty `focus_mode/data/models/` and `focus_mode/data/repositories/` directories; updated `session_tracker_screen.dart` recent sessions list to show per-type icons and colors (matching `session_history_screen` convention); added `plannedDurationMinutes` column to CSV export output; all 890+ tests pass
- 2026-05-15: Completed `internationalisation_master.md` remaining items (A.3, A.4, C.3) — Made `correctKeywords`, `incorrectKeywords`, `exerciseKeywords` required params in `ConversationManager` and `TutorService.startLesson` to eliminate English-only default keyword lists; updated all callers (`tutor_screen.dart`) and tests (`conversation_manager_test.dart`, `tutor_screen_test.dart`) to pass localized keywords from `AppLocalizations`; verified `sendTooltip` is already required (C.1) and `lesson_progress_bar.dart` already uses `l10n.overtimeLabel` (C.2); verified no duplicate ARB keys via existing `scripts/validate_arb_no_duplicates.dart` (A.4); added CI validation documentation to `docs/i18n.md`; regenerated l10n; `dart analyze` zero errors
- 2026-05-15: Completed `test_master.md` (Critical Test Gap: MentorService Planning Intent & Boilerplate-Only Provider Tests) — Added repository-throws test to `mentor_service_test.dart` verifying `_checkAndHandlePlanningIntent` silently catches exceptions (bare `catch (_)`); rewrote `mentor_providers_test.dart` (removed 6 Riverpod framework-behavior tests, added `mentorProgressTrackerProvider` wiring test via `mentorAttemptRepositoryProvider` override and `mentorModelIdProvider` default-value test); rewrote `practice_providers_test.dart` (removed 14 framework-behavior tests, kept 4 minimal type-check tests); rewrote `dashboard_providers_test.dart` (removed override/singleton boilerplate, kept 5 type/wiring tests with `dashboardStudyProgressTrackerProvider` wiring verification); rewrote `focus_mode_providers_test.dart` (removed 3 framework tests, kept wiring and resolution tests — `focusSessionServiceProvider` already verified `service.repository` wiring); all 58 affected tests pass
- 2026-05-15: Completed `code_refactor_master.md` (File Placement Violations) — Moved `dashboard_models.dart` from `presentation/models/` to `data/models/`; moved `practice_models.dart` from `presentation/models/` to `data/models/`; moved `practice_data_service.dart` and `practice_session_service.dart` from `presentation/services/` to `services/`; moved `mentor_action.dart` and `progress_report.dart` from `models/` to `data/models/`; renamed `subject_feature.dart` → `subjects.dart` and `questions_feature.dart` → `questions.dart`; added `accessibility_preferences.dart` export to `settings.dart` barrel; extracted `IterableExtension` from `core.dart` into `extensions/iterable_extensions.dart` with re-export; consolidated duplicate `practice_models_test.dart` into `data/models/` location; `git mv` history preserved on all renames; all existing tests pass
- 2026-05-15: Completed `internationalisation_master.md` (Architectural Bottlenecks & Spanish Translation Quality) — Created `lib/core/config/locale_config.dart` with `AppLocale` enum as single source of truth for supported locales; refactored `main.dart` `supportedLocales`/`localeResolutionCallback`, `profile_screen.dart` dropdown, and `l10n.yaml` to derive from `AppLocale`; fixed `planAdherence` Spanish grammar (`cumplimiento al Plan` → `Cumplimiento del Plan`); added `timeConflict` ARB key (EN/ES) and replaced 2 hardcoded strings in `lesson_booking_sheet.dart` and `planner_providers.dart`; removed duplicate `badgeCenturyName`/`badgeCenturyDesc` keys from both ARBs; normalized all 105 Spanish `@description` fields in `app_es.arb` to English; updated `docs/i18n.md` with regional variant infrastructure, duration abbreviation policy, and ARB description convention; removed stale badge test assertions; regenerated l10n; `flutter gen-l10n` zero errors
- 2026-05-15: Completed `ui_ux_master.md` (Responsive Layout & Dashboard Accessibility) — Fixed `CollapsibleCard`: replaced fixed padding with `ResponsiveUtils.cardPadding`; added `Semantics(headingLevel: 2, button: true, expanded: ...)` to collapse toggle. Fixed `WeakAreasCard` and `TopicBreakdownCard`: removed inner `Card` wrapper eliminating double-card artifact. Fixed `SummaryRow`: removed hardcoded `maxWidth < 400` breakpoint, uses `ResponsiveUtils.breakpointOf` with breakpoint-aware column count (2/3/4). Fixed `DashboardHeader`: added `Semantics(headingLevel: 1)`. Fixed `AnimatedBarChart`: made `barWidth` responsive via `LayoutBuilder` with 24px minimum; `borderRadius` adapted to breakpoint. Fixed `ChatBubble`: symmetric horizontal padding on both sides for uniform inter-bubble spacing. Fixed `ConversationInput`: uses `ResponsiveUtils.screenPadding` horizontally. Added `Semantics(headingLevel: 3)` to all dashboard card titles (`_cardTitle`, `PlanAdherenceCard`, `BadgesCard`, `MasteryProgressCard`, `WeeklyChart`, `TopicBreakdownCard`). `dart analyze` zero errors, all 89 widget tests + 17 barrel tests pass
- 2026-05-15: Completed `future_functionality_planner.md` remaining fixes — Fixed MentoreService pending action pipeline (`_checkAndHandlePlanningIntent` now includes `topicId`, `subjectId`, `scheduledTime`, `durationMinutes` in payload for `ActionExecutor` to execute properly; `suggestReschedule` includes full reschedule payload with `originalSessionStart`); removed `PlanAdherenceCard` from dashboard barrel (planner is the adherence hub, removing metric duplication); added `PlanAdapter.getDailyAdherenceFeedback()` for per-day real-time feedback after each session; added 3 new ARB keys (`adherenceLowToday`, `adherencePartialToday`, `adherenceExceededToday`) with ES translations; added corresponding methods in `LocalizationService`; regenerated l10n; `dart analyze` zero errors, all 41 mentor service tests + 16 dashboard barrel/adherence tests pass
- 2026-05-15: Completed `internationalisation_master.md` (Hardcoded English strings in 5 screens) — replaced hardcoded strings in `dashboard_screen.dart` (8 card titles) with `l10n.*` keys (`summary`, `focusTime`, `weeklyActivity`, `planAdherence`, `masteryOverview`, `weakAreas`, `topicPerformance`, `achievements`); replaced hardcoded strings in `settings_screen.dart` Focus Mode section (`focusMode`, `focusTime`, `focusTimerDescription`, `dailyStudyCap`, `noLimit`) and `_getDailyCapLabel`/`_showDailyCapDialog` to use `l10n.minutesValue`/`l10n.noLimit`; replaced hardcoded strings in `planner_screen.dart` (`studentIdOptional`, `subjectIdHint`); replaced hardcoded strings in `llm_task_manager_screen.dart` (5 token/status labels with `tokenUsageSummary`, `totalTokens`, `totalCost`, `l10n.done`, `l10n.failed`); added 10 new ARB keys (`summary`, `noLimit`, `focusTimerDescription`, `dailyStudyCap`, `tokenUsageSummary`, `totalTokens`, `totalCost`, `failed`, `subjectIdHint`) to both `app_en.arb` and `app_es.arb` with Spanish translations; regenerated l10n; `dart analyze` zero issues, ARB key parity tests pass
- 2026-05-15: Completed `test_master.md` — AC#3: Created `topicRepositoryProvider` in `subjects/providers/`, `lessonRepositoryProvider` and `tutorSessionRepositoryProvider` in `lessons/providers/` for Riverpod-native DI; refactored `TopicListScreen`, `LessonListScreen`, `LessonDetailScreen` to use `ref.read()` instead of constructor injection; refactored 3 lesson presentation test files to use `ProviderScope` with `overrides` (consistent with practice feature convention); AC#1: Added `AppErrorHandler.handleError` to `lesson_list_screen.dart` and `lesson_detail_screen.dart` (were silently catching), added error-state widget tests to all 3 lesson screen tests verifying `SnackBar`+retry; AC#2: Added navigation-verification widget tests to all 3 lesson screen tests (topic→lesson-list, lesson→lesson-detail, app-bar/bottom-bar teaching-mode→tutor); AC#4: Fixed no-op test in `topic_list_screen_test.dart` to assert meaningful content; AC#5: Merged unique `NavigatorObserver` FAB test from `practice_test.dart` into `practice_screen_test.dart`, removed duplicate `practice_test.dart`; AC#6: Added `mentor_action_test.dart` (5 tests: creation, defaults, equality) and `progress_report_test.dart` (6 tests: fields, defaults, weak-topics, badges, equality); deleted obsolete `_FakeLessonRepository` parameter-based API from e2e test; all 93 affected tests pass
- 2026-05-15: Completed `internationalisation_master.md` — Sections 1-4 of Spanish ARB audit confirmed already fixed (register, anglicisms, dialect, terminology); fixed 39 auto-generated descriptions in `app_en.arb` and 1 in `app_es.arb` (Section 6); added ~70 new ARB keys (EN+ES) for badge names/descriptions, notification strings, engagement nudges, plan reasons/explanations, recommendations, suggestions, and share text; created `LocalizationService` at `lib/core/services/localization_service.dart` bridging ARB localizations into non-widget services with comprehensive badge/nudge/notification/plan/recommendation/suggestion lookups; updated `NotificationService` to accept `LocalizationService` and produce localized notification titles/bodies/channel names (Section 5b); updated `EngagementScheduler` nudge messages to use localized strings (Section 5c); updated `PlanAdapter` adherence messages to use localized strings (Section 5e); updated `StudyProgressTracker.getRecommendations` to return localized recommendation text (Section 5f); updated `AdaptivePracticeEngine.getTopicRecommendations` to return localized suggestions (Section 5g); updated `PersonalLearningPlanService` to use localized plan reasons and focus labels (Section 5d); updated `StudyProgressTracker.getBadges` to return localized badge names/descriptions via `LocalizationService` (Section 5a); updated `session_export_service.dart` share text with localized fallback (Section 5h); documented `l10n.yaml` and `main.dart` locale resolution for Latin American Spanish target (Section 7); regenerated l10n; `flutter analyze` zero issues
- 2026-05-15: Completed `ui_ux_master.md` navigation & planner i18n fixes — replaced Focus bottom-nav tab with Dashboard tab (dashboard serves as hub with Planner navigation card); added Planner entry point as quick-link card on Dashboard; fixed dead "N more..." scheduled-lessons button to navigate to lesson list; added explicit Semantics labels to main-screen FAB and DailyPlanCard IconButtons; replaced all hardcoded strings in `planner_screen.dart`, `lesson_booking_sheet.dart`, `pending_action_card.dart`, `collapsible_card.dart` with `AppLocalizations` lookups; replaced hardcoded `Colors.green`/`Colors.orange` with `colorScheme` theme colors in `milestone_timeline.dart`, `roadmap_card.dart`, `pending_action_card.dart`; added 21 new ARB keys (EN+ES) for planner/dashboard i18n coverage; regenerated l10n; `flutter analyze` zero new errors
- 2026-05-15: Completed `code_refactor_master.md` remaining fixes — Renamed `save(T)`→`create(T)` in 9 repos (badge, engagement_nudge, pending_action, plan_adherence, plan, roadmap, subject, conversation, tutor_session) to resolve `invalid_override` conflicts with base `Repository.save(String, T)`; extracted `PlanAdherenceMetric` and `MasteryImprovementMetric` models from `instrumentation_service.dart` into `core/data/models/plan_adherence_metric_model.dart` and `core/data/models/mastery_improvement_metric_model.dart`; updated all adapter imports to point to new model files; fixed `subject_repository.dart` internal `save(updated)` calls to `save(subjectId, updated)`; updated 17 mock/fake `save` methods across test files to `create`; `flutter analyze` zero repo-layer errors, 950+ tests pass
- 2026-05-14: Completed `code_refactor_master.md` (Repository Layer Architectural Refactor) — Relocated all 19 repositories from `lib/core/data/repositories/` to feature directories (practice: 4, planner: 5, subjects: 3, teaching: 2, dashboard: 1, lessons: 1, sessions: 1, ingestion: 1, questions: 1); created base `Repository<T>` class at `lib/core/data/repository.dart` with `openBox`, `put`, `get`, `getAll`, `delete`, and `filterBy`; created `HiveBoxNames` constants class at `lib/core/data/hive_box_names.dart` eliminating all 24 hardcoded box name strings; fixed `topic_repository.dart` `addParent` bug (discarded `copyWith` result); removed dead `questionsToBoxSafe` and `countAllQuestions` methods from `SpacedRepetitionQueries`; removed duplicate `Logger` class from `llm_usage_meter.dart`; created `lib/features/focus_mode/focus_mode.dart` barrel file and added export to `features.dart`; updated all feature barrel files with data repo exports; updated `core/data/data.dart` to export `repository.dart` and `hive_box_names.dart` instead of old repo exports; updated 66+ import sites across `lib/` and `test/` directories; relocated 18 test files to `test/features/*/data/repositories/`; rewrote `subject_repository_test.dart` to use mock-class pattern (new `SubjectRepository` no longer has nullable-box constructor); removed old `test/core/data/repositories/` directory; `dart analyze` zero errors
- 2026-05-14: Completed `future_functionality_planner.md` (Triple-Redundant Validation/Mastery & Disconnected LLM Infrastructure) — AC#1: Removed `MasteryGraphService.evaluateAnswer` (delegated to `AnswerValidationService` as single source); AC#2: `AdaptivePracticeEngine.updateQuestionState` already a no-op delegating to `MasteryGraphService.recordAttempt`; AC#3: Wired `LlmTaskManager`/`LlmUsageMeter` into all 6 provider methods (`_callOpenRouter`, `_streamOpenRouter`, `_callOllama`, `_streamOllama`, `_callOpenAI`, `_streamOpenAI`) with consistent task lifecycle (`_initTask`, `_completeTask`, `_failTask`) and `recordUsage`; AC#4: Removed duplicate `_messages` list from `ConversationManager` — `_memory` is now single source of truth, messages derived via getter; AC#5: Removed hardcoded `_defaultModels` from `ApiConfig`, made `defaultModelId` required parameter in `QuestionGenerationService`; AC#6: Deleted `llm_service.dart` barrel file; removed `_mockChatResponse`, `_getMockQuestions`, `_getMockLessonBlocks`, `_getMockLesson`, `_mockValidateAnswer`, `_mockStudyPlan`, `generateQuestions`, `generateLessonBlocks`, `generateLesson`, `validateAnswer`, `generateStudyPlan` and all associated parsing methods from `llm_chat_service.dart`; removed unused model imports and `Logger`; removed dead `ConversationMemory` inline class from `llm_chat_service.dart` (already extracted to separate file); `dart analyze lib/` zero errors, 5 service tests + 25 question generation tests pass (2 pre-existing failures)
- 2026-05-14: Completed `test_master.md` (Repository Test Fragmentation) — verified subject repository already consolidated to single test file (1282 lines, organized via `group()` blocks for getAll/get/save/delete/getWithTopics/addTopicToSubject/removeTopicFromSubject/getByCode/Constructor/Uninitialized/Box error propagation/Data integrity/Edge cases/Stress and bulk operations/init); removed duplicate `themeModeEnum getter` group from `settings_repository_hive_test.dart` (already in shared helper); moved `toString` and `api key isolation` groups from both InMemory and Hive settings repo test files into `settings_repository_test_helper.dart`; verified `repository_test.dart` does not exist (no action needed); confirmed no other repository test files demonstrate fragmentation (19 files, 1 per repository); all 252 affected tests pass (59 InMemory + 71 Hive + 122 subject)
- 2026-05-14: Completed `test_master.md` — created `test/features/dashboard/presentation/models/dashboard_models_test.dart` with 40 unit tests covering all 7 model classes (`MasterySnapshot`, `OverallStats`, `WeeklyTrendEntry`, `FocusTodayStats`, `AdherenceData`, `BadgeDisplay`, `DashboardArgs`); tested `fromMap` with valid maps, missing keys, and null values; tested `isEmpty` getters for empty/non-empty states; tested `FocusTodayStats.hours` formatting for edge values (0, 3599, 3600, 1800, 9000, 86400 seconds); tested `BadgeDisplay` default/custom category; tested `DashboardArgs` constructor; `dart analyze` zero errors, all 40 tests pass
- 2026-05-14: Completed `code_refactor_master.md` — verified all 6 listed issues already resolved (AnswerValidationService wrapper already removed, PracticeResultsScreen already in correct directory, LessonService params already clean, hardcoded fallback options already fixed, ExportSection already uses Riverpod, bottom sheet shape already centralized). Further improvements in `lib/features/mentor/`: replaced hardcoded `'openai/gpt-4o-mini'` model ID in `mentor_screen.dart` with `mentorModelIdProvider` reading from `settingsProvider.selectedModel`; added `mentorPendingActionRepoProvider` and `mentorModelIdProvider` to `mentor_providers.dart` for proper DI; added 3 provider tests (resolution, override, singleton) for `mentorPendingActionRepoProvider`; all 39 mentor tests pass
- 2026-05-14: Completed `ui_ux_master.md` font-size, reduceMotion, and nav-layout issues — fixed `effectiveFontSize` clamp in `main.dart` from `(14, 30)` to `(10, 30)` to match slider range so the user's full choice is respected (AC: theme is sole authority, `TextScaler.noScaling` already in place); replaced `WidgetsBinding.instance.platformDispatcher.accessibilityFeatures.reduceMotion` with `MediaQuery.disableAnimationsOf(context)` in `FocusTimerWidget` pulse animation (didUpdateWidget, build, didChangeDependencies) for widget-tree-reactive accessibility gating; added `didChangeDependencies` to reactively stop/start pulse when `disableAnimations` changes; fixed `PracticeSessionNavButtons` test to correctly assert Row vs Column per breakpoint with position-based checks; `dart analyze` zero errors, focus timer 30/30 tests pass, nav buttons 6/6 tests pass
- 2026-05-14: Completed `future_functionality_planner.md` (Dashboard Architecture Overhaul) — Verified all 10 ACs are met: per-card Riverpod `FutureProvider.family` providers for 7 data sources, parallel initialization via `Future.wait`, typed data models (`MasterySnapshot`, `OverallStats`, `WeeklyTrendEntry`, `FocusTodayStats`, `AdherenceData`, `BadgeDisplay`), `CollapsibleCard` with Hive-persisted `DashboardLayoutPreferences`, `EmptyDashboardChecklist` for new users, card-level error/loading/retry per `AsyncValue`, no `NumericFocusOrder`, `ExportSection` using `share_plus`; added `asyncValue`/`onRetry` to `WeakAreasCard` and `TopicBreakdownCard` collapsible cards; removed nested `Card` from `PlanAdherenceCard` (avoids double-wrapping inside `CollapsibleCard`); fixed empty state detection to include `adherenceData.isEmpty` and use comprehensive `isLoading` check across all 7 providers; added typed `DashboardArgs` class with `studentId` field replacing `Map<String, dynamic>` route arg; fixed `BadgesCard` tests (removed stale `Card`/`SizedBox` finder expectations); fixed pre-existing `dashboard_screen_test.dart` failures (added `topicRepo: FakeTopicRepository()` to plan adherence and adherence metric groups); `dart analyze` zero errors, 52 widget/provider/barrel tests pass
- 2026-05-14: Completed `test_master.md` — Reduced `dashboard_providers_test.dart` from 128 to 54 lines (2 focused tests verifying provider resolution and override behavior); verified all 12 previously-missing test files now exist with meaningful tests (focus_mode providers, lesson widgets, planner widgets, practice widgets/services, subject model at correct location); confirmed `dashboard_screen_coverage_test.dart` already removed; confirmed `DashboardDataLoader` already has unit tests covering all partial-failure paths (focus failure, mastery failure, adherence failure, combined failures)
- 2026-05-14: Completed `internationalisation_master.md` full sweep — Issue 1: replaced hardcoded 'Dashboard' tooltip with `l10n.dashboard` and hardcoded 'Focus' nav label with new `l10n.focus` key (added to both ARB files); Issue 2: fixed Spanish formality register (`focusForMinutes`: Enfócate→Enfóquese, `dailyLimitReachedBody`: Has alcanzado tu→Ha alcanzado su, Descansa→Descanse, vuelve→vuelva); Issue 3: copied English `@description` values from `app_en.arb` for 13 keys in `app_es.arb` (markschemeUnavailable, answerTooShort, goodResponseLength, answerTooShortForCredit, noDrawingDetected, invalidDrawingData, allStepsIdentified, specialHandlingRequired, someAnswersIncorrect, correctAnswerIs, allStepsFormat, partialStepsFormat, noStepsFormat, allRequiredStepsMissing); Issue 4: translated `roadmaps` from English loanword to `Rutas de aprendizaje`; regenerated l10n; all 260 l10n tests pass
- 2026-05-14: Completed `code_refactor_master.md` (FocusSessionService/FocusSessionRepository consolidation) — Verified all AC: `focusSessionRepositoryProvider`/`focusSessionServiceProvider` exist only in `focus_mode_providers.dart` (single source of truth, `init()` removed from provider build since `HiveInitializer` eagerly opens box); duplicate declarations removed from `app_providers.dart`; `dashboardFocusServiceProvider` removed from `dashboard_providers.dart` (dashboard reads canonical provider); `FocusSessionRepository._box` changed from `late` field initialized in `init()` to direct getter `Hive.box<String>('focus_sessions')` (box already opened by `HiveInitializer`); `engagement_scheduler.dart` uses constructor-injected `FocusSessionService?`; screens no longer call `.repository.init()`; updated repository test to explicitly open the box in setUp; `dart analyze` zero errors, all 151 precondition tests pass
- 2026-05-14: Completed `ui_ux_master.md` (Settings notification toggles) — Added 4 Hive fields to `SettingsBox` (`revisionRemindersEnabled`, `lessonNotificationsEnabled`, `overworkAlertsEnabled`, `planAdjustmentNotificationsEnabled` with Hive fields 14–17); updated `SettingsBox.g.dart` adapter; updated `SettingsRepository.getSettings/updateSettings` to read/write new fields; added `SettingsController.updateRevisionReminders/updateLessonNotifications/updateOverworkAlerts/updatePlanAdjustmentNotifications` in `app_providers.dart`; wired all 4 dead `SwitchListTile.onChanged` callbacks in `settings_screen.dart` to dedicated provider actions with proper persisted state; removed redundant "Daily Reminders" child switch (was duplicating master `studyRemindersEnabled` control with identical subtitle); removed duplicate "Study Reminders" switch from "Study Preferences" section (same `studyRemindersEnabled` field, dual-control pattern); fixed `buildSettingsScreen` in `settings_screen_test.dart` to include `AppLocalizations` delegates with `Locale('en')`; added `FakeSettingsRepository implements SettingsRepository` with missing `clearSettings`/`getApiKey` methods; added 9 notification-toggle widget tests (visibility when enabled/disabled, master toggle state, 4 sub-toggle persistence); `dart analyze` zero errors, 44 repository unit tests + 14 SettingsBox unit tests pass
- 2026-05-14: Completed `test_master.md` — Rewrote `practice_providers_test.dart` (18 tests: resolution, overrides, singleton, container isolation, lifecycle); created `lessons/providers/lesson_providers_test.dart` (15 tests: FutureProvider.family data/empty states, llm fallback pattern throws UnimplementedError, masteryService override/wiring); created `lessons/services/lesson_service_test.dart` (30 tests: getLessonsForStudent, getLessonsByTopic, getTopicsWithLessons dedup, getLessonCountBySubject, getCompletionRate with empty/all/mixed/zero, getTotalStudyMinutes with endTime/null, getRemainingLessonCount with edge cases, getProgressBySubject with multi-subject, getRecentLessons with limit, getUpcomingLessons filtering/sorting); added `content_pipeline_test.dart` processAndClassify tests (6 tests: classify call count, topic match, classification failure, no topic match, field passthrough, repo failure); added `session_export_test.dart` writeCSVFile/writeJSONFile/writePDFFile tests (6 tests: file extension, content matching); refactored `SessionExportService` with `@visibleForTesting writeCSVFile/writeJSONFile/writePDFFile` for testable payload generation; all 96 affected tests pass
- 2026-05-14: Completed `internationalisation_master.md` full sweep — Issue 1: removed 12 duplicate top-level ARB keys from `app_es.arb` (masteryLevelDeveloping/Browsing/Proficient/Expert/Novice, noTopicDataYet, achievements, topicPerformance, thisWeek, studyTime, exportCsv, masteryOverview); fixed masteryLevelDeveloping conflict (Desarrollando→En Desarrollo); Issue 2: fully localized `progress_export_service.dart` — added 45 new CSV/PDF ARB keys (EN+ES), replaced all hardcoded English strings in CSV column headers, PDF section titles, table headers, empty states, share text, and mastery levels with `l10n.*` getters; updated all 4 call sites (shareComprehensiveCSV/JSON/PDF, exportComprehensiveCSV/PDF) to accept/pass `AppLocalizations`; Issue 3: verified focus mode already fully localized with 16 focus-mode ARB keys in both locales; Issue 4: added `labelJson` ARB key (EN+ES), replaced hardcoded `Text('JSON')` in `session_history_screen.dart` with `Text(l10n.labelJson)`; Issue 5: applied 4 Spanish refinements (planAdherence: Cumplimiento→Adherencia; drawingWithStrokes: Dibujando→Dibujo; considerUsingPieChart/BarChart: lowercase graph-type names); regenerated l10n; all 312 l10n tests pass
- 2026-05-14: Completed `code_refactor_master.md` feature directory restructure — removed 17 empty placeholder directories across dashboard, lessons, llm_tasks, planner, quickguide, sessions, subjects; moved 7 widget files from top-level `widgets/` to `presentation/widgets/` (lessons: 2, planner: 4, sessions: 1); renamed `questions/ui/` to `questions/presentation/` (4 files); moved 5 corresponding test files to `presentation/widgets/`; updated all import paths in barrel files, lib source files, and test files; updated AGENTS.md convention to `presentation/widgets/`; `dart analyze` passes with zero errors
- 2026-05-14: Completed `test_master.md` — fixed FocusSessionService fake (rewrote FakeFocusSessionRepository to preserve/retrieve session data, added non-zero stats tests, removed dead Hive.init, replaced flaky Future.delayed timer test with fake_async); added 40 dashboard widget tests (9 widgets: badges_card, dashboard_header, export_section, mastery_progress_card, plan_adherence_card, summary_row, topic_breakdown_card, weak_areas_card, weekly_chart + dashboard_barrel + providers); added 72 practice widget/model/service tests (practice_models, practice_session_nav_buttons, practice_results_screen, practice_empty_state, practice_feedback_widget, practice_mode_card, practice_mode_grid, practice_mode_option, practice_mode_sheet, practice_session_stats_bar, subject_practice_card, subject_selection_sheet, topic_selection_sheet, weak_areas_sheet, spaced_repetition_sheet, question_type_localizer, practice barrel); upgraded skeleton tests (dashboard_barrel → 10 export assertions, practice_test → 19 export assertions); fixed structural violations (created settings_box_test.dart, accessibility_preferences_test.dart at proper locations, practice barrel test at feature root); added core service tests (student_id_service, mastery_calculation_service with 15 tests, llm_task_manager with 10 tests); all 221 tests pass
- 2026-05-14: Completed `future_functionality_planner.md` — Phase 1: Extracted `PlannerService` from 854-line `PlannerScreen` monolith into `lib/features/planner/services/planner_service.dart`; created Riverpod `PlannerNotifier`/`PlannerState` in `planner_providers.dart`; extracted 4 reusable widgets (`PlanSummaryCard`, `DailyPlanCard`, `RoadmapCard`, `MilestoneTimeline`) into `widgets/`; rewired `PlannerScreen` as `ConsumerStatefulWidget` with Riverpod; Phase 2: Removed fragile keyword-matching (`_isScheduleRequest`, `_isConfirmation`, `_isRejection`, `_executePendingAction`) from `MentorService`; replaced in-memory `_pendingAction`/`_pendingConfirmation` with persisted `PendingActionModel`/`PendingActionRepository`; delegation model detects scheduling intent post-LLM-response via `_checkAndHandlePlanningIntent`; Phase 3: Created `LessonService` with CRUD, progress tracking, and subject-based lesson analytics; created `lesson_providers.dart` with Riverpod `FutureProvider.family` providers; Phase 4: Created persistent `BadgeModel`/`BadgeRepository`/`BadgeService` with `BadgeDefinitions` for 6 badge types; removed ephemeral `getBadges()` computation from `StudyProgressTracker` (delegated to `BadgeService`); added `showBadgeUnlocked` notification; Phase 5: Removed hardcoded `studentId = 'default'` from `EngagementScheduler`; replaced fixed 9AM timer with configurable `EngagementSchedulerConfig`; created `EngagementNudgeModel`/`EngagementNudgeRepository` for nudge persistence; replaced `InstrumentationService`-based adherence in `_countConsecutiveLowAdherence` with `PlanAdherenceRepository`; Phase 6: Integrated `SpacedRepetitionRepository` persistence into `AdaptivePracticeEngine.updateQuestionState`; Phase 7: Created unified `PlanAdherenceModel`/`PlanAdherenceRepository`; added `recordDailyAdherence` method to `PersonalLearningPlanService`; removed `AdaptivePracticeEngine` import from `PersonalLearningPlanService` (unused field); updated barrel files; all 7 phases with zero analysis warnings
- 2026-05-14: Completed `internationalisation_master.md` Issues 1-6 — added localised `quickGuideSystemPrompt` ARB key (EN/ES) and replaced hardcoded English system prompt in `quick_guide_screen.dart`; replaced English-only fallback keyword matching with unconditional `fallbackGeneralResponse`; fixed `mentorProgressReportTitle` informal "Tu"→"Su" register; unified `masteredLabel` (Adquirido→Dominados) and `weakAreasAccuracy` (Áreas Débiles→Áreas por mejorar) for consistency; added AI localisation checklist to `docs/i18n.md`; added explicit `supported-locales` to `l10n.yaml`; updated tests and regenerated l10n
- 2026-05-14: Completed `code_refactor_master.md` (subjects feature refactor) — verified all 7 issues are fully implemented: SubjectRepository moved to `core/data/repositories/` (no core→feature imports), `SubjectColors` dead wrapper removed (all call sites use `ColorUtils` directly, test migrated to `ColorUtils` group), `getStudentSubjects` removed (method no longer exists), all 4 screen+Args pairs (SubjectDetail, PracticeSession, LessonDetail, LessonList) accept args directly (no field destructuring in router), hardcoded strings replaced with `l10n` keys, `mounted` guards added to `_startPractice` and `_confirmDelete`, naming convention unified to `*Screen`/`*_screen.dart` across all 3 subject presentation files; empty `features/subjects/data/` scaffolding cleaned up
- 2026-05-14: Completed `ui_ux_master.md` — replaced QuickGuide's inline `MessageListWidget` Container with shared `ChatBubble` (fixes user bubble color, asymmetric border radii, textTheme.bodyMedium, avatars, sender labels, animated typing dots); added `CallbackShortcuts` (Ctrl+Enter) + `FocusTraversalGroup` + `Semantics` params to shared `ConversationInput` and replaced QuickGuide's `MessageComposerWidget` with it; removed `message_composer_widget.dart` and `typing_indicator_widget.dart`; aligned QuickGuide's `ConversationMemory` maxTurns from 30→20 (shared default); removed unused `google_fonts` dependency from `pubspec.yaml`; replaced hardcoded `'/mentor'` with `AppRoutes.mentor` and all other hardcoded route strings in `settings_screen.dart`; added `explicitChildNodes: true` and `ExcludeSemantics` to mode nav cards; replaced hardcoded `EdgeInsets.all(12)` with `ResponsiveUtils.cardPadding` in mode nav cards; wrapped Dashboard FAB in `Focus` for keyboard accessibility; fixed `chat_bubble_test.dart` missing `AppLocalizations` delegates and `AnimatedBuilder` expectation; fixed QuickGuide screen tests for `ProviderScope` wrapper and `Icons.send_rounded`; deleted obsolete test files for removed widgets
- 2026-05-14: Completed `future_functionality_planner.md` Issue 5 — Study Timer / Focus Mode with Pomodoro-style countdown, break enforcement, daily cap, Hive persistence, dashboard focus-time card, Settings link, bottom-nav entry, and focus-aware overwork detection in EngagementScheduler; created 6 new files: `focus_session_model.dart`, `focus_session_repository.dart`, `focus_session_service.dart`, `focus_timer_widget.dart`, `session_summary_card.dart`, `focus_timer_screen.dart`; modified 8 files: `hive_initializer.dart`, `app_router.dart`, `app_providers.dart`, `dashboard_providers.dart`, `dashboard_screen.dart`, `engagement_scheduler.dart`, `settings_screen.dart`, `main.dart`
- 2026-05-14: Completed `ui_ux_master.md` (Focus Mode systemic design) — replaced all hardcoded English strings in `focus_timer_screen.dart`, `focus_timer_widget.dart`, and `session_summary_card.dart` with `AppLocalizations.of(context)` lookups; replaced all `Colors.green`/`Colors.orange`/`Colors.grey.shade*` with `Theme.of(context).colorScheme.primary`/`tertiary`/`onSurfaceVariant`/`error`/`primary` equivalents; made timer circle responsive via `LayoutBuilder` (min 200px, max 80% available width, clamped at 260px); wrapped countdown text in `FittedBox(fit: BoxFit.scaleDown)` so `01:23:45` long-format strings do not overflow; fixed pulse animation to stop when `isPaused` is true (added `!widget.isPaused` check in `didUpdateWidget`); created `focus_mode/providers/focus_mode_providers.dart` with Riverpod `focusSessionRepositoryProvider` and `focusSessionServiceProvider`; refactored `FocusTimerScreen` to read providers via `ref.read`/`ref.watch`; added `FakeFocusSessionRepository`/`FakeFocusSessionService` for test overrides; added `ProviderScope` with overrides to all widget tests; replaced `Colors.green` icon in daily-limit dialog with `colorScheme.primary`; added setup→active→break flow widget test verifying session lifecycle; all 106 tests pass
- 2026-05-14: Completed `internationalisation_master.md` — replaced hardcoded 'Upload Content'/'Dashboard' in `subject_detail_view.dart` with l10n calls; added `dashboard` ARB key to both EN/ES ARB files; fixed Spanish register in 23 mentor strings from informal (tú) to formal (usted); fixed capitalization in `noLessonsUsePlanner`/`noTopicsYetAddSome`; fixed comma in `fillAllFieldsCorrectly`; fixed `masteryLevelBrowsing` from "Iniciado" to "Explorando"; created `question_type_localizer.dart` extension mapping `QuestionType` enum values to localized labels; replaced `question.type.name` with localized labels in `practice_session_screen.dart` and `practice_session_question_card.dart`; regenerated l10n
- 2026-05-14: Completed `code_refactor_master.md` — replaced all hardcoded English strings in `answer_validation_service.dart` with `AppLocalizations` via updated `ValidationMessages` and 14 new ARB keys (markschemeUnavailable, answerTooShort, goodResponseLength, answerTooShortForCredit, noDrawingDetected, invalidDrawingData, allStepsIdentified, specialHandlingRequired, someAnswersIncorrect, correctAnswerIs, allStepsFormat, partialStepsFormat, noStepsFormat, allRequiredStepsMissing); decomposed `practice_screen.dart` from 872→337 lines (extracted 8 widgets: SubjectPracticeCard, PracticeEmptyState, PracticeModeGrid, SubjectSelectionSheet, PracticeModeSheet, TopicSelectionSheet, SpacedRepetitionSheet, WeakAreasSheet); decomposed `practice_session_screen.dart` from 618→341 lines (extracted 3 widgets: PracticeSessionStatsBar, PracticeSessionQuestionCard, PracticeSessionNavButtons); updated practice barrel file (`practice.dart`) to export all new widget files; added ES translations for all 14 new ARB keys; regenerated l10n code
- 2026-05-14: Completed `ui_ux_master.md` final sweep — replaced hardcoded `Colors.red`/`Colors.green` in `upload_screen.dart` error/success containers with `Theme.of(context).colorScheme.errorContainer`/`primaryContainer`/`error`/`primary`; added `FocusTraversalOrder` + `Semantics` to input method row (ChoiceChips → ActionChip for camera); removed duplicate `retry` key from `app_en.arb`/`app_es.arb`; verified all 7 `upload_screen_test.dart` and 174 `app_localizations_test.dart` tests pass
- 2026-05-14: Completed `test_master.md` — fixed `\sqrt`/`\frac` parser bug in `math_expression_widget.dart` (was consuming `{` as part of command name); replaced 3 shallow RichText-existence tests with 3 content-verification tests (sqrt symbol, operator color, isSolution container); deleted old misplaced test files (`test/features/questions/models/markscheme_model_test.dart`, `test/features/questions/services/answer_test.dart` — now only at `test/core/data/models/` and `test/core/services/`); created `test/features/ingestion/presentation/upload_screen_test.dart` with 7 widget tests (form validation, pipeline integration, success/error states, URL mode toggle); verified existing cache eviction tests in `answer_validator_test.dart` and existing ContentPipeline/LlmTaskManagerScreen tests; updated coverage script already covers `test/features/`
- 2026-05-14: Completed `internationalisation_master.md` — fixed `questionsAndMinutes`/`topicQuestionsAndMinutes` Spanish abbreviation (Q→P); replaced hardcoded `'0 min 0 sec'` English fallback in `practice_session_screen.dart` with `formatDurationFromContext`; replaced manual `HH:MM:SS` in `llm_task_manager_screen.dart` with `DateFormat.Hms(l10n.localeName)`; passed locale to `DateFormat('E')` in `session_analytics.dart`; replaced `drawingWithStrokes` fragile `{plural}` placeholder hack with proper ICU plural syntax in both `app_en.arb`/`app_es.arb` and updated widget/test call sites; added `localeResolutionCallback` to `MaterialApp` for country-code fallback; regenerated localizations via `flutter gen-l10n`
- 2026-05-14: Completed `internationalisation_master.md` — fixed `mentorDaysAgo` ICU plural in `app_en.arb`/`app_es.arb` (was `{daysCount} days ago`, now `{daysCount, plural, =1{1 day ago} other{{daysCount} days ago}}` with matching Spanish `=1{hace 1 día}`); added `questionsCountMetric` Spanish ICU plural coverage test; verified all 136 localization coverage tests pass; moved issue to `issues/completed/`
- 2026-05-14: Completed `ui_ux_master.md` hardcoded color sweep — replaced `Colors.green`/`orange`/`red` with `AppTheme.progressColor()` in `dashboard_screen.dart` (accuracy MetricCard, adherence metric, mastery stats, weak areas, badges); replaced `Colors.blue`/`teal`/`purple` with `colorScheme.primary/tertiary/secondary` in `dashboard_screen.dart` MetricCard accents; replaced `Colors.green`/`orange`/`grey` with `colorScheme.primary/tertiary/onSurfaceVariant` in `lesson_list_screen.dart` status icon and chip; replaced `Colors.grey.shade300` with `colorScheme.outline` and `Colors.grey` with `colorScheme.onSurfaceVariant` in `single_answer_widget.dart` border and radio icon; replaced `Colors.blue.shade400`/`Colors.grey.shade300` with `colorScheme.primary/outline` in `canvas_drawing_widget.dart` canvas border; replaced `Colors.grey.shade200/600/700` and `Colors.white` with `colorScheme.outlineVariant/onSurfaceVariant/surface` in `canvas_drawing_widget.dart` grid, hint text, icon button background/color; parameterized `GridPainter` with `gridColor` from theme; replaced `Theme.of(context).primaryColor` with `colorScheme.primary` in `session_history_screen.dart` (5 usages: filter buttons, leading icon, trailing text, MetricCard accent) and `practice_session_screen.dart` (3 usages: question type badge, `_buildMiniStat`); replaced `theme.cardColor`/`theme.dividerColor` with `colorScheme.surfaceContainerHighest`/`outlineVariant` in `animated_bar_chart.dart`; replaced `Colors.grey` fallbacks with `colorScheme.onSurfaceVariant` in `animated_bar_chart.dart`; replaced `theme.disabledColor` with `colorScheme.onSurfaceVariant` in `session_history_screen.dart` empty state icon
- 2026-05-13: Completed `internationalisation_master.md` — extracted 12 hardcoded error messages from `handlers.dart` to ARB keys (with proper ICU placeholders), replaced `const Text('Retry')` with localized `l10n.retry`; localized 25+ labels in `analytics_dashboard.dart` (section headings, metric labels, mastery levels, empty states, export buttons); localized 4 mode-card strings in `quick_guide_screen.dart` ('AI Tutor', 'Mentor', subtitles); localized 3 sender labels in `chat_bubble.dart` ('You', 'Tutor', 'System'); localized progress bar labels in `lesson_progress_bar.dart` (remaining min, question count, correct count); localized initial greeting and summary stats in `tutor_screen.dart`; localized welcome message body and error response in `mentor_screen.dart`; localized snackbar error in `practice_screen.dart`; fixed 5 Spanish translation inconsistencies (`accuracy`→'Precisión', `weakLabel`→'Por mejorar', `atRiskTopics`→'Temas con dificultades', `weakAreas`→'Áreas por mejorar', `masteredLabel`→'Adquirido'); fixed 2 Spanish informal register violations (`lessonTimeEnded`, `errorOccurred`); fixed English ICU plurals for `randomQuestions`, `sessionsCount`, `questionsCountLabel`, `questionsCountMetric` in `app_en.arb`; added 50+ new ARB keys with Spanish translations; added key-parity coverage test (`app_en.arb` ↔ `app_es.arb`); updated `docs/i18n.md` with comprehensive "Adding a new locale" checklist and Spanish translation consistency guidelines
- 2026-05-13: Completed `code_refactor_master.md` — consolidated `ProfileData` (typeId 5) into `UserProfile` (typeId 10, renamed `avatarUrl`→`avatarIcon`), removed `ProfileData` class and adapter, updated all consumers (settings_repository, profile_screen, main.dart) and removed stale `_typeIdProfileData` from hive_type_ids; moved `subject_model.dart` from `subjects/models/` to `subjects/data/models/` and fixed all imports (lib + test); extracted mutable business logic from `MasteryState` into immutable `MasteryCalculationService` with pure-function `recordAttempt` (all fields now `final`, removed mutation methods); extracted hardcoded LLM token pricing constants into configurable `TokenPricingConfig` class; removed empty `lessons/data/models/` scaffolding directory; updated settings barrel file (`settings.dart`) to re-export models and repository; fixed `getStudentSubjects` dead parameter documentation; updated all test files for changed APIs
- 2026-05-13: Completed `ui_ux_master.md` Issue 1-7 — centralized routing with `onGenerateRoute` and typed `AppRouter`/`AppRoutes` (replaced `MaterialPageRoute` pushes with `pushNamed` across 9 screens); added `onGenerateRoute` with fade transitions to `main.dart`; added `AppTheme.progressColor/urgencyColor/priorityColor/masteryColor` shared utilities in `app_theme.dart`; fixed `gridCrossAxisCount` return type `double→int` in `responsive.dart`; replaced all remaining hardcoded `Colors.*` with `Theme.of(context).colorScheme` tokens in analytics_dashboard, learning_plan_dashboard, settings_screen, profile_screen, practice_screen, practice_session_screen, session_tracker_screen, session_history_screen, subject_list_view, planner_screen; replaced blocking loading dialog in `_showAiModelSelection` with inline `_AiModelLoadingSheet` StatefulWidget; fixed `_SessionEndDialog` button style (`ElevatedButton→FilledButton`); fixed `AnimatedBarChart` re-animation from zero by converting to StatefulWidget with `_hasAnimated` flag; fixed `ChatBubble` typing indicator with proper `_TypingIndicator` StatefulWidget using `AnimationController`+`AnimatedBuilder` for staggered dot animation; fixed subject card layout shift in `practice_screen.dart` (unified card design); fixed inline breakpoints (`width>400` → `ResponsiveUtils.breakpointOf().isMobile`) in session_history_screen, analytics_dashboard, planner_screen; removed duplicate `Semantics` wrappers around `ListTile`/`SwitchListTile`/`NavigationDestination` widgets across settings_screen and main.dart; fixed `practice_session_screen.dart` results screen to use `textTheme.headlineMedium`; clamped `childAspectRatio` to prevent layout overflow
- 2026-05-13: Completed `test_master.md` teaching feature coverage — refactored `TutorScreen` to accept injectable `TutorService` via optional constructor parameter for testability; added `test/features/teaching/presentation/tutor_screen_test.dart` with 6 widget tests (loading indicator, topic title in app bar, progress bar after init, message list, end lesson button, conversation input); relocated 4 orphaned test files to proper feature directories (`test/validation/answer_test.dart`, `test/repository/repository_test.dart`, `test/screens/practice_test.dart`, `test/models/llm_models_test.dart`); replaced low-value inline Flutter-primitive widget tests in `lesson_presentation_test.dart` and `topic_list_screen_test.dart` with meaningful model/helper function tests that exercise real application code
- 2026-05-13: Implemented three foundational pillars per `future_functionality_planner.md` — Content Ingestion, Smart Planner, Analytics Dashboard; replaced hardcoded `'anonymous'` student ID with auto-generated UUID via `StudentIdService`; created `lib/features/ingestion/` module with upload screen (paste text/URL) and `ContentPipeline` orchestration; updated `Source` model with `subjectId`, `topicId`, `syllabusId`, `sourceUrl`, `studentId`, `language`, `summary` fields and enhanced `SourceRepository` with query methods; removed silent mock fallback from `PdfIngestionService` (now returns `Result` errors); created `PlanRepository` for Hive persistence of `PersonalLearningPlan`; rewrote `PlannerScreen` to use `PersonalLearningPlanService` with plan caching; connected `PlanAdherenceTracker.recordDay()` in `SessionTrackerScreen._endSession()`; added plan data to `MentorService.getSchedule()`; created `lib/features/dashboard/` module with unified `DashboardScreen` combining plan adherence, mastery overview, weekly trends, topic breakdown with human-readable names, weak areas with practice button, badges, and CSV export; enhanced `AnimatedBarChart` with y-axis label, value tooltips, and responsive layout; added dashboard FAB and `/dashboard` route; added upload access from subject detail; added `ingestion` and `dashboard` exports to `features.dart`; fixed all hardcoded `'anonymous'` references (tutor_screen, practice_screen, lesson_list_screen, session_tracker_screen, personal_learning_plan_service, study_progress_tracker default params)
- 2026-05-13: Fixed Spanish formal register violations per `internationalisation_master.md` — corrected 6 strings in `app_es.arb` (quickGuideWelcomeMessage, quickGuideHelpContent, fallbackExplainResponse, fallbackQuizResponse, fallbackMathResponse, fallbackGeneralResponse) from informal *tú* to formal *usted* register; deduplicated `quickGuide` key in `app_en.arb`; updated Spanish assertions in `app_localizations_coverage_test.dart` and `app_localizations_test.dart` to match corrected ARB values; refactored `localeProvider` in `app_providers.dart` from per-language `if` chain to `AppLocalizations.supportedLocales` iteration; replaced hardcoded language-subtitle ternary in `profile_screen.dart` with locale-to-label map; added formal-register PR review checklist to `docs/i18n.md`; regenerated localizations via `gen_l10n.sh`
- 2026-05-13: Fixed UI/UX issues per `ui_ux_master.md` — made `SliverAppBar.expandedHeight` proportional to screen height via `MediaQuery` in `subject_detail_view.dart`; replaced `as dynamic` casts on `StudySession`/`StudySession` with proper typed access; replaced hardcoded `Colors.green`/`orange`/`red` score indicators with theme-derived `colorScheme.primary`/`tertiary`/`error` across `subject_detail_view.dart` and `practice_session_screen.dart`; removed all `FittedBox(fit: BoxFit.scaleDown)` from `question_card_widget.dart` chip labels and multiChoice options, using `TextOverflow.ellipsis` instead; replaced hardcoded `Colors.blue`/`orange`/`purple`/`red` mode-card colors in `practice_screen.dart` with `colorScheme.primary`/`tertiary`/`secondary`/`error`; replaced `EdgeInsets.all(16)` with `ResponsiveUtils.cardPadding(context)` in `animated_bar_chart.dart`; replaced hardcoded `Colors.grey.shade*` with `colorScheme.onSurfaceVariant`/`surfaceContainerHighest` across practice screens; centralized DI by creating `lib/core/providers/app_providers.dart` — moved `database`, `settingsRepository`, `SettingsController`, and all provider definitions (`settingsProvider`, `apiKeyProvider`, `apiBaseUrlProvider`, `selectedModelProvider`, `localeProvider` etc.) out of `main.dart`; removed `import 'package:studyking/main.dart'` from 9 feature files (`settings_screen.dart`, `api_config_screen.dart`, `profile_screen.dart`, `subject_management_screen.dart`, `topic_list_screen.dart`, `lesson_list_screen.dart`, `lesson_detail_screen.dart`, `tutor_screen.dart`, `mentor_screen.dart`) replacing with imports from `app_providers.dart`
- 2026-05-13: Major architectural cleanup — consolidated 3 incompatible `Result<T>` types into single sealed class at `lib/core/errors/result.dart`; removed duplicate `core/common.dart` barrel; added cascade `init()` to `DatabaseService`; extracted `SecurityConfig`, `AppConfig`, `AppConstants` from bloated `app_runtime_config.dart` and removed dead classes (`StudyConfig`, `PdfConfig`, `ErrorKeys`, `MediaConfig`); fixed layering violation by removing `../../features/subjects/` export from `core/data/data.dart`; standardized all repository `init()` signatures to `Future<void>` using pre-opened `Hive.box<T>()`; deleted empty barrel stubs (`lessons/services`, `planner/services`, `sessions/services`), empty `hive_repository.dart`, duplicate `session_repository.dart`, and its orphaned test; fixed silent `catch` in `practice_screen.dart` with proper logging + user feedback; replaced `const bool.hasEnvironment('flutter.debug')` with `kDebugMode` in `AppErrorHandler._logError`
- 2026-05-13: Added 17 unit tests for `QuestionPDFGenerator` covering `addQuestion`, `setMetadata`, `generate`, `exportToJSON`, `exportToCSV`, `generatePracticePDF`, `generateAnswerKeyPDF`, `generateWithAnswers`, and `clear` — closes coverage gap for `lib/core/services/pdf_generator/question_pdf_generator.dart`
- 2026-05-13: Added `mounted` guard test for `LearningPlanDashboard._loadData` — verifies `setState` is not called after widget is disposed by using a delayed `Completer`-based fake
- 2026-05-13: Fixed 2 broken core `AnswerValidationService` tests — canvas test now expects `isCorrect: false` (string input is not valid canvas data); essay test uses >50 char input to match actual validation logic
- 2026-05-13: Completed `test_master.md` coverage gaps — added feature-layer `AnswerValidationService` tests for `validateWithMarkscheme` (static, 4 scenarios), `validateEssayAnswerWithMarkscheme` (static, 4 scenarios), `validateStepByStepWithMarkscheme` (static, 3 scenarios) in `test/features/questions/services/answer_validator_test.dart`; removed empty `lib/features/practice/services/` directory
- 2026-05-13: Implemented test coverage improvements per `test_master.md`: added `studySessionRepositoryProvider` in `practice_providers.dart` and injected it into `PracticeSessionScreen` to support mocking; added `FakeStudySessionRepository` and session auto-save widget test (verifying `_sessionAutoSaved` guard and `StudySessionRepository.create`); added `FakeMasteryGraphService` and `masteryGraphServiceProvider` override in `practice_screen_test.dart`; added widget tests for disabled mode-card taps, topic-selector flow with available topics, and weak-areas practice path (session launch and no-weak-topics snackbar)
- 2026-05-13: Enhanced `PracticeSessionScreen` tests — replaced minimal spaced repetition mode test with three dedicated tests verifying `_updateNextReview` is called with correct masteryLevel (0.8 correct, 0.2 incorrect) and confirming it is NOT called when `isSpacedRepetition` is false; made `UpdateNextReviewCall` public in `shared_test_helpers.dart` to support these assertions; removed empty `test/features/practice/services/` directory; fixed `answer_validator_test.dart` canvas-drawing null-markscheme test expectation to match core validation logic
- 2026-05-13: Enhanced `math_expression_widget.dart` — replaced basic tokenizer with full expression parser supporting LaTeX commands (`\sqrt`, `\frac`, `\alpha`–`\omega`, `\times`, `\cdot`, `\pm`, `\rightarrow`/`\leftarrow`, `\infty`, `\ge`/`\le`/`\neq`), brace-grouped superscripts/subscripts rendered as `WidgetSpan`, numeric literal styling (deep orange), improved operator colors (green for +/-, orange for ×/÷, blue for relational), and brown-delimited parentheses
- 2026-05-13: Fixed `lesson_detail_screen.dart` timer — added `_startTimer()` with 1-second `Timer.periodic` so elapsed time displays correctly instead of always showing zero
- 2026-05-13: Improved `StudyProgressTracker.getTopicMasteryLevel()` — corrected fallback logic to match `MasteryState._updateMasteryLevel()`: returns `'Novice'` for 0 attempts (was incorrectly `'Browsing'`), `'Browsing'` for ≥1 attempt, `'Developing'` for ≥3 attempts ≥60%, `'Proficient'` for ≥5 attempts ≥80%, `'Expert'` for ≥10 attempts ≥90%
- 2026-05-13: Replaced mock-data CSV exports in `StudyProgressTracker` — `exportQuestionsAndAttemptsCSV()` and `exportSessionHistoryCSV()` now accept `studentId` and produce real attempt/mastery data instead of placeholder rows
- 2026-05-13: Fixed `MentorService` duplicate `import 'dart:async'` line
- 2026-05-13: Implemented conversational AI tutor (Teaching Mode) with adaptive pacing — enhanced `ConversationManager` with exercise evaluation, consecutive correct/incorrect tracking, adaptive pace adjustment (0.5x–1.5x), and `adaptiveReview` phase for struggling students; refactored `TutorScreen` to use reusable `ConversationInput` widget and show adaptive pace stats in lesson summary
- 2026-05-13: Implemented Mentor Mode with schedule awareness, intent detection, and confirmation gating — enhanced `MentorService` with `_handleScheduleRequest`, `_handleProgressRequest`, `_handleInactivityCheck` methods; added `pendingConfirmation`/`pendingAction` for safe schedule changes; refactored `MentorScreen` to use `ConversationInput` widget
- 2026-05-13: Built analytics dashboard (`AnalyticsDashboard`) with summary metrics cards, weekly activity bar chart, topic performance breakdown, mastery overview with progress bars, achievements display, session history stats, and CSV export buttons
- 2026-05-13: Enhanced `QuickGuideScreen` with streaming support (`chatStream`), `ConversationMemory`, proper `ConversationMessage` model, mode navigation cards to AI Tutor/Mentor
- 2026-05-13: Created reusable `ConversationInput` widget in `core/widgets/` with loading state, hint text, and customizable leading/trailing actions
- 2026-05-13: Fixed `StudyProgressTracker.getTopicMasteryLevel()` — now uses real `MasteryGraphService` data with accuracy-based fallback instead of always returning 'Browsing'; made `MasteryGraphService` non-optional with default instance
- 2026-05-13: Fixed `StudyProgressTracker.getWeeklyTrend()` — replaced hardcoded `'student_1'` with configurable `studentId` parameter
- 2026-05-13: Implemented full i18n infrastructure overhaul — fixed locale switching (language dropdown now updates `localeProvider` and `MaterialApp.locale` reactively); added device locale auto-detection on first launch with saved profile override; fixed duplicate `"medium"` ARB key (renamed to `fontSizeMedium`/`difficultyMedium`) across `app_en.arb`, `app_es.arb`, and all Dart references; normalised Spanish register to formal *usted* (su/sus possessives, formal imperatives) across ~15 ARB strings; fixed Spanish examples (ej. → p. ej., culturally adapted names); documented `color_utils.dart` fallback path; created `docs/i18n.md` and `scripts/gen_l10n.sh` for adding future languages
- 2026-05-13: Replaced all `debugPrint` calls with `Logger` abstraction — removed `debugPrint` from `logger.dart` (now uses `print()`) and test helper widget, resulting in zero `debugPrint` usage across the entire codebase
- 2026-05-13: Refactored `settings_screen.dart` model selection to use `ModelListingService` instead of raw `http.get` — removed duplicate `_AiModel` class, `_parseModels` method, and `package:http`/`dart:convert` dependencies from the settings screen
- 2026-05-13: Added `test/providers/llm_engine_provider_test.dart` with 27 unit tests for `LLMAIEngineProvider` covering initial state, API key management, model selection, usage tracking, model fetching, and error handling; made `OpenRouterClient` injectable in `LLMAIEngineProvider` to support testing
- 2026-05-13: Consolidated answer validation into single `AnswerValidationService` in `lib/core/services/` — removed `isMatch()`/`_isSimilar()` from `Markscheme` and `QuestionEvaluation` models; deleted `evaluation_adapter_service.dart`, `practice/services/answer_validation_service.dart`; refactored `mastery_graph_service.dart` to use unified service; removed dead question engines (`question_engine.dart`, `question_engine_dynamic.dart`) and unreferenced prototype files (`lesson_scheduler_engine.dart`, `graph_rendering_engine.dart`, `graph_type_detector.dart`); unified LLM model definitions in `lib/core/data/models/` (`llm_config.dart`, `llm_models.dart`); refactored `LlmService` with provider-agnostic `chat()` method and token tracking; migrated `question_generation_service.dart` to use `LlmService` instead of direct HTTP calls; moved `markscheme_model.dart` from features to `lib/core/data/models/` to fix reverse dependency; completed `features.dart` to export all 8 feature barrel files; added `services/` barrel files to `lessons/`, `sessions/`, `planner/`
- 2026-05-13: Implemented i18n sweep — replaced hardcoded English strings in `quick_guide_screen.dart` (welcome message, suggested prompts, semantics labels, fallback responses) and `practice_session_screen.dart` (timer format) with `AppLocalizations` calls; added locale-aware duration separator in `time_utils.dart`; fixed `formatDate` calls without l10n in `session_tracker_screen`, `subject_detail_view`, `session_history_screen`; fixed Spanish `focusOnMistakes` register (formal → informal); added `durationSeparator` key to ARB files; updated l10n tests to match corrected English/Spanish expectations
- 2026-05-13: Migrated theme system from Material 2 to Material 3 — set `useMaterial3: true`, migrated `CardTheme`, `ElevatedButtonTheme`, and `AppBarTheme` to M3 tokens, and added `NavigationBarThemeData` for consistent M3 `NavigationBar` styling
- 2026-05-13: Added `ThemeMode.system` option to the theme picker bottom sheet, allowing users to follow device-level dark/light preference
- 2026-05-13: Removed `FittedBox` with `BoxFit.scaleDown` from Quick Guide chat bubbles — long messages now wrap naturally and respect the user's accessibility font size
- 2026-05-17: Completed `dry_run_usability_validator.md` (remaining items) — **Focus Mode:** Added `PopScope` to `FocusTimerScreen` with confirmation dialog (Stay/End Session) when back is pressed during active focus session. **M3 Fix:** Refactored `PracticeSessionScreen._onWillPop()` — extracted `_finalizeSession()` to avoid double-pop from `_navigateToResults()`; user now sees PracticeResultsScreen inline without route pop. **M10/Exam:** Added `PopScope` and `_onWillPop()` to `ExamSessionScreen` with confirmation dialog during active exam. **M11:** Source practice sheet shows `sourceWithNoQuestions` label for zero-question sources; zero-question source tap triggers upload suggestion dialog. **L10n:** Added `confirmExitFocus`, `confirmExitFocusBody`, `endSession`, `sourceWithNoQuestions` ARB keys with EN/ES translations. **Tests:** Added 3 popscope tests to focus_timer_screen_test, 3 popscope tests to exam_session_screen_test, 3 popscope tests to practice_session_screen_test, 1 zero-question test to source_practice_sheet_test. All new tests pass.
- 2026-05-13: Unified `centerTitle` — set globally to `false` in `AppTheme.appBarTheme` and removed per-screen overrides in `practice_session_screen.dart` and `session_history_screen.dart`
- 2026-05-13: Replaced hardcoded `TextStyle`, `Color`, and other visual constants with `textTheme` and `colorScheme` lookups across `api_config_screen.dart`, `profile_screen.dart`, `settings_screen.dart`, `practice_session_screen.dart`, `session_history_screen.dart`, and `subject_detail_view.dart`
- 2026-05-13: Implemented systemic responsive layout, accessibility, and touch-target (WCAG 2.5.5/2.5.8) improvements — added `ResponsiveUtils.loaderInTouchTarget()` and `ensureMinTouchTarget()` helpers; replaced 20x20 `CircularProgressIndicator` in app bars with 48x48 touch-target containers in `profile_screen.dart`, `subject_management_screen.dart`, `subject_selection_screen.dart`; replaced hardcoded `EdgeInsets.all(16)` with `ResponsiveUtils.screenPadding` in `api_config_screen.dart`, `settings_screen.dart`, `learning_plan_dashboard.dart`, `practice_screen.dart`; replaced card/item margins with responsive vertical spacing in `subject_detail_view.dart`, `session_tracker_screen.dart`, `lesson_list_screen.dart`, `lesson_detail_screen.dart`, `topic_list_screen.dart`, `quick_guide_screen.dart`, `single_answer_widget.dart`; added `FocusTraversalGroup`/`FocusTraversalOrder` to `profile_screen.dart`, `settings_screen.dart`, `subject_management_screen.dart`, `planner_screen.dart`; added `CallbackShortcuts` with Ctrl+Enter in `quick_guide_screen.dart`; added Escape-key dialog close intent/action via `MaterialApp.shortcuts`; added `MergeSemantics` around avatar icon+label in `profile_screen.dart`; ensured font size composes with system `textScaleFactor` in `main.dart` (font is always at least the system-scaled size); ensured `MediaQuery.boldText/highContrast/textScaler` are consumed in `main.dart` builder
- 2026-05-14: Completed `code_refactor_master.md` — removed 4 orphaned/dead files (`app_features.dart` with `FeatureFlagService`/`AppFeature`, `learning_plan_dashboard.dart`, `analytics_dashboard.dart`, `subject_management_screen.dart`), removed dead `AdaptiveMetrics` class from `conversation_manager.dart`, removed dead `FormulaWidget` from `math_expression_widget.dart`, cleaned up all cross-references (app_constants.dart export, app_config.dart featureFlags field/bootstrap params/AppConstants.initialize, 5 test files); created shared `llmServiceProvider` in `lib/core/providers/llm_providers.dart` to centralize LLM init; converted `MentorScreen`, `TutorScreen`, `QuickGuideScreen` from `StatefulWidget` to `ConsumerStatefulWidget` — replaced hardcoded `apiKey: ''` with `ref.read(llmServiceProvider)` so AI screens read the user-configured API key from `apiKeyProvider` instead of silently degrading to mock data
- 2026-05-14: Completed `test_master.md` structural cleanup — consolidated `analytics_test.dart` and `session_analytics_test.dart` into `test/features/sessions/widgets/session_analytics_test.dart`; moved `session_tracker_screen_test.dart` and `session_history_screen_test.dart` to `test/features/sessions/presentation/` for consistent `presentation/` subdirectory across all feature tests; fixed missing imports in `test/integration/e2e_test.dart` (LessonRepository, TutorSessionRepository, TutorSession)
- 2026-05-14: Completed `future_functionality_planner.md` items 1,3,4,6,7 — removed redundant `lib/core/widgets/canvas_drawing_widget.dart` (no remaining imports); created `RoadmapModel`/`MilestoneModel` (`roadmap_model.dart`) and `RoadmapRepository` with Hive persistence; enhanced `PlannerScreen` with tabbed UI (Study Plan + Roadmaps tab), roadmap creation dialog, interactive milestone timeline/Gantt visualization with progress bars; created `NotificationService` (`flutter_local_notifications`) with channels for general reminders, revision nudges, overwork warnings, plan adjustments, lesson reminders, low-mastery alerts, and daily scheduling; wired `EngagementScheduler` to `NotificationService` for automated OS-level delivery of revision, overwork, plan-adjustment, and low-mastery notifications; added `ProgressExportService` with comprehensive CSV/JSON/PDF export including overall stats, topic mastery breakdown, attempts history, weekly trend, and badges; added comprehensive export options to `SessionHistoryScreen` popup menu; added `Token Usage Summary` meter to `LlmTaskManagerScreen` with total tokens, cost, done/failed counts, per-task token badges; added notification preference toggles to `SettingsScreen` (daily reminders, revision, lesson, overwork, plan adjustment); added 30+ new localization keys to `app_en.arb`/`app_es.arb` and generated files for roadmap, notification, and export features; added `flutter_local_notifications` dependency
- 2026-05-14: Completed `future_functionality_planner.md` — Issue 1: Fixed `_countConsecutiveLowAdherence` stub in `engagement_scheduler.dart` to use real `InstrumentationService.getAdherenceHistory`; wired plan adaptation nudge through `getPlanAdjustmentNudge` with consecutive low-adherence detection; added `dailyPlanTarget`/`noPlanForToday`/`planAdjustmentSuggested` ARB keys and planner integration; Issue 2: Added "Test Connection" button to `api_config_screen.dart` with latency reporting; fixed `registerCancelCompleter` in `llm_task_manager.dart` to properly store `cancelCompleter` on task; Issue 3: Enhanced `EngagementScheduler` with real `_countConsecutiveLowAdherence` implementation, removed duplicate local `Logger` class; Issue 4: Confirmed canvas drawing widget already integrated in `question_card_widget.dart` (`_buildCanvasContent`); added voice input toggle (`_toggleVoiceInput`) and image capture button (`_pickImage`) stubs with `comingSoon` feedback to `tutor_screen.dart` using `ConversationInput.leading`; added camera capture option (`_captureFromCamera` via `image_picker`) to `upload_screen.dart`; added `voiceInput`/`captureImage`/`camera` ARB keys with EN/ES translations; Issue 6: Replaced 30+ hardcoded English strings in `dashboard_screen.dart` with `AppLocalizations` (section headings, metric labels, mastery levels, snackbars, weak areas, export buttons); replaced 12+ hardcoded strings in `upload_screen.dart` (labels, hints, chips, error/success messages); replaced 10+ hardcoded strings in `planner_screen.dart` (plan summary chips, day cards, tooltips, error messages); replaced 6+ hardcoded strings in `llm_task_manager_screen.dart` (title, active count, empty state, model/started/ended labels, cancel button); added 60+ new ARB keys with Spanish translations; deduplicated `inProgress` ARB key
