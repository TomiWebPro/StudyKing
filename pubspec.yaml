name: studyking
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.4.11

  # Database
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Networking
  dio: ^5.4.0

  # UI packages
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.8

  # Form handling
  # form_builder_validators_localized: ^3.0.0

  # Drawing/canvas
  # flutter_canvas_drawer: ^2.0.2

  # Math rendering
  # flutter_latex: ^3.0.0

  # Storage
  path_provider: ^2.1.2
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.11

flutter:
  uses-material-design: true
