# Issue-Finder Run — 2026-08-25

`flutter analyze` is clean (0 warnings/errors). De-duplicated against 88 existing open issues.

## New issues opened (5)

| # | Title | Label |
|---|---|---|
| 89 | agent_loop sends malformed tool-result string with stray `}` | bug |
| 90 | agent_loop unknown-tool message interpolates `_ParsedResponse` object instead of `toolName` | bug |
| 91 | conversation_manager re-yields full buffer each chunk, duplicating streamed tutor text | bug |
| 92 | QuestionRepository.getQuestionsWithMarkschemes ignores `subjectId`, returns cross-subject results | bug |
| 93 | SessionMigrationService loses focus-session duration (reads non-existent `actualDurationSeconds`) | bug |

## How each was found
- #89 / #90: manual read of `lib/core/services/llm_agent/agent_loop.dart` — string interpolation defects in tool-call error/result assembly.
- #91 / #92 / #93: delegated parallel source audit of under-reviewed modules (teaching, sessions, questions, flashcards, focus_mode, mentor tools) and manual verification of each candidate against the actual code.

## Duplicates skipped
None of the 5 overlap with the 88 existing open issues (verified by title scan). Existing conventions work (inline Logger, Result<T>, empty-catch, toStringAsFixed, .normalized) remains covered by prior issues #9, #11, #12, #35, #39.

## New issues opened (second run, 5)

| # | Title | Label |
|---|---|---|
| 94 | Canvas-drawing answers always scored incorrect (validateStatic passes empty list) | bug |
| 95 | Step-based answer scoring undercounts perfect answers (wrong denominator) | bug |
| 96 | Focus inline-practice session saved with hardcoded 5-min duration and wrong field | bug |
| 97 | Tutor-generated question difficulty derived from student score (wrong/inverted field) | bug |
| 98 | getTopicProgress filters by questionId.contains(topicId) substring match | bug |

## How these were found
- Delegated a source audit subagent to read lib/ for concrete defects not in the 93 existing issues; manually verified each candidate against the actual code (file:line) before opening.
- #94: answer_validation_service.dart:307 passes `[]` instead of `answer` to validateCanvasDrawing.
- #95: answer_validation_service.dart:140 multiplies by `totalPoints/steps.length` twice; perfect score understated.
- #96: focus_timer_screen.dart:1069-1075 hardcodes startTime-5min, duration 5, and writes topicIds into subjectIds.
- #97: tutor_service.dart:531 sets difficulty from `evalResult.score` (student performance) instead of question difficulty.
- #98: study_progress_tracker.dart:110 filters attempts by `questionId.contains(topicId)` substring rather than real topic association.

## Notes
- Analyze clean; no new lints.
- Scope capped at 5 to avoid spam.
