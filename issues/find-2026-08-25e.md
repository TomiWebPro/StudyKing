# Issue-finder run — 2026-08-25 (run e)

Scope: `flutter analyze` (45 errors / 12 warnings, all in `test/`), grep for convention
deviations and missing tests, compared against the 124 already-open issues.

## New issues opened (3)

### #125 — lesson_recap source files missing — tests fail to compile (45 analyzer errors)
- bug
- Tests for the lesson-recap feature were committed but the source files
  (lesson_recap_model/adapter/repository/service) do not exist, so `flutter analyze`
  fails with 45 errors and the test suite does not compile.
- Related: #29.

### #126 — Remaining screens instantiate repositories/services directly (Hive box leaks, untestable)
- enhancement (refactor)
- Extends #119 (Subject/Settings) and #40 (Dashboard providers). Lists 12+ screens
  that `new` repositories/services inline (often in build/loops) and never close the
  Hive box, blocking fake-based widget tests.

### #127 — Add missing unit test for plan_context_repository
- enhancement (test)
- Only remaining uncovered source file after #100/#101. Per AGENTS.md placement table.

## Duplicates / skipped
- exam_session_service `ExamResult.fromJson` unguarded casts -> already #112.
- `ReviewLogEntry.fromJson` / model `DateTime.parse` unguarded -> covered by #71.
- `study_plan_tab` / `mentor_service` / `session_tracker_screen` `int.parse` on user/LLM
  text -> verified SAFE (guarded by `int.tryParse` validators / `\d+` regex).
- `session_export_service` `toStringAsFixed` -> CSV path, allowed per AGENTS.md (invariant en).
- Many screen-level direct `Repository()` instantiations partially overlap #119/#40/#32/#36.

## Verification performed
- `flutter analyze` captured (57 issues total, all 45 errors + 12 warnings in test/ files).
- Confirmed lesson_recap source files absent while tests present.
- Confirmed plan_context_repository has no test file.
- Grepped existing 124 open issues to avoid duplicates before creating.
