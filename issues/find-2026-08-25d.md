# Issue Finder Run — 2026-08-25 (run d)

Toolchain: ready. `flutter analyze` reported **No issues found!**

## Issues opened (NEW, non-duplicate)

| # | Title | Label | Notes |
|---|---|---|---|
| 115 | Duplicate SessionQueryService class definition in lesson_service.dart (dead code / build hazard) | enhancement | `lesson_service.dart:6` duplicates `session_query_service.dart:6`. Verified identical via diff. |
| 116 | OnboardingStorage.getBool catch returns hard-coded true instead of defaultValue | bug | `lib/features/onboarding/services/onboarding_storage.dart:11-21` catch returns `true`, ignores `defaultValue`; `as bool` can throw on non-bool stored value. |
| 117 | Subject stats tab overstates mastery by counting all-subject topics | bug | `lib/features/subjects/presentation/widgets/subject_stats_tab.dart:67-71` counts mastery across ALL subjects, so ratio can exceed 1.0. |
| 118 | Seed-topic IDs can collide across subjects created in the same millisecond | bug | `lib/features/subjects/presentation/subject_selection_screen.dart:110,124` ID suffix `_0` reset per subject → collision + Hive overwrite. |
| 119 | Subject/Settings screens instantiate repositories directly instead of using Riverpod providers | enhancement | Extends #36/#40 to subject_detail_screen, subject_selection_screen, subject_history_tab, subject_stats_tab, settings_screen. |

## Candidates reviewed and SKIPPED (already exist)
- `getWeeklyTrend` week windows overlap / double-counting boundary-day attempts → already filed as **#83**.
- Swallowed `Result` failures via `.data ?? []` in flashcards/subjects/lessons repos → covered systemically by **#28**.
- `result.data!` force-unwraps in `lesson_agent_service.dart:92` and `slide_deck_generator.dart:130,219` → verified guarded by `if (result.isFailure || result.data == null)` immediately above, NOT bugs.
- Direct repository instantiation on SubjectListScreen (#36) and Dashboard (#40) → already filed.

## Context
- `flutter analyze` clean; no TODO/FIXME/HACK except a false positive in ES localization ("TODOS LOS INTENTOS").
- No empty `catch (_) {}` blocks remain in lib/.
- 100+ open issues already cover most systemic patterns (Result<T>, force-unwraps, jsonDecode, number formatting, model routing, planner, mentor, ingestion, llm_tasks). Focused on under-covered features: lessons, subjects, onboarding, settings.
- Note: GitHub labels are limited to bug/enhancement/etc.; `refactor`/`test` labels do not exist, so refactor-style items were filed under `enhancement`.
