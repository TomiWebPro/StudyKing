# Issue Finder Run — 2026-08-25

Agent: ISSUE-FINDER (opencode/hy3-free)
Repo: TomiWebPro/StudyKing
Toolchain: READY

## Context
Prior runs (find-2026-08-23 / find-2026-08-24 / find-2026-08-25) had already opened
issues #4–#103. This run focused on finding NEW, concrete, implementable bugs not
covered by any existing open issue, prioritizing lib/ logic bugs over generic
"add tests" / "cleanup" duplicates.

## Scan method
- `flutter analyze` → 3 issues total, all in test files (1 compile error:
  `planner_service_multi_plan_test.dart:32` undefined `syllabusGoals`; 2 unused
  imports). The lib/ tree compiles clean.
- grep for `toStringAsFixed` (all remaining uses are CSV/LLM/log/invariant-en
  per AGENTS.md — not user-facing violations), `catch(_){}` (none),
  `.toLowerCase()` (only legitimate file-extension use).
- Delegate subagent broad-scan of 20 untested service files for Result
  violations, unguarded parses, logic bugs, and i18n issues.
- Verified each candidate file:line before filing.

## Issues OPENED (5, all new — #104–#108)
1. #104 — LessonAgentService: LessonBlock.lessonId set to topicId while Lesson
   uses separate Uuid id → blocks orphaned / never load. (lesson_agent_service.dart:97,131-243)
2. #105 — slide_deck_generator: substring(start>end) RangeError on bad LLM
   chapter indices silently drops the ENTIRE deck. (slide_deck_generator.dart:144-147)
3. #106 — question_variant_service: greedy regex `\[.*\]` over-captures and
   jsonDecode throws → all variants lost. (question_variant_service.dart:150-151)
4. #107 — concept_map_generator: array-form LLM response treated as {} → null
   concept map. (concept_map_generator.dart:84-85)
5. #108 — PersonalLearningPlanService: force-unwrap `l10n!` crashes instead of
   returning Result.failure. (personal_learning_plan_service.dart:927,985,987)

## Candidates considered and DROPPED (not filed)
- mentor_context_builder raw `'${stats[...]}'` strings: numbers feed the LLM
  prompt (LLM-facing), en-invariant is explicitly allowed by AGENTS.md → not a bug.
- syllabus_resolver `Result.failure(l10n?.x)` null error value: lower severity,
  overlapping spirit with #28 (surface swallowed Result failures).
- planner_service_multi_plan_test.dart compile error + 2 unused imports: real,
  but narrower than the 5 lib bugs chosen and related to ongoing test-lint work (#8).
- 442 lib files still lack tests: covered in spirit by existing #7 (add missing
  test files); opening a duplicate would be spam.

## Duplicates skipped
No new issue duplicated an existing open issue (#4–#103). Verified via
`gh issue list --state open --limit 100` before creation.

## Notes
- Did NOT edit application code, AGENTS.md, or agent_must_read.md.
- Only this notes file is committed.
