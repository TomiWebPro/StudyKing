# Issue-finder run 2026-08-23

Toolchain was READY. No open issues existed prior (gh issue list returned empty).

## Issues opened (5)

| # | Title | Label |
|---|---|---|
| 1 | Non-exhaustive switch on ProcessingStatus (missing generatingFlashcards case) | bug |
| 2 | get_syllabus_structure_tool indexes a String as a Map (suggestedNext['topicId']) | bug |
| 3 | Stale test fakes break compilation (StudyProgressTracker.getDailyTrend, ContentPipeline.processFullPipeline) | enhancement (no `test` label exists) |
| 4 | Fix unawaited Future returns inside try blocks (unawaited_return_in_try_block) | enhancement |
| 5 | Migrate deprecated share_plus API (Share/shareXFiles) to SharePlus.instance | enhancement |

## Duplicates skipped
None — gh issue list returned no open issues.

## Notes
- `flutter analyze` reported 69 issues (errors + warnings + info). Real compile `error`s targeted: 3x non_exhaustive_switch_statement, 2x argument_type_not_assignable (issue #2), 2x test compile errors (issue #3).
- Warnings/info (deprecations, unawaited futures, build-context-across-async, prefer_function_declarations in tests) were grouped into issues #4 and #5; remaining lower-priority lints left for future runs to avoid spam.
- Conventions checked (AGENTS.md): Result<T> return types, static final Logger, .normalized, locale-aware number formatting, test placement, hand-written fakes. No violations beyond the test-fake drift in issue #3 were raised as new issues this run.
- `git push` of this notes file is the only commit performed; no application code was modified.

---

## Issue-finder run (second pass) 2026-08-23

Toolchain READY. Prior open issues #1-#5 already existed and were NOT duplicated.

### Issues opened (4)

| # | Title | Label |
|---|---|---|
| 6 | Guard BuildContext used across async gaps (use_build_context_synchronously) | bug |
| 7 | Add missing test files for 27 uncovered source files | enhancement |
| 8 | Resolve remaining flutter analyze lints in test files | enhancement |
| 9 | Convert public service/repository methods to Result<T> per AGENTS.md | enhancement |

### Duplicates skipped
All of #1-#5 (non-exhaustive switch, get_syllabus_structure_tool String/Map, stale test fakes, unawaited Future in try, deprecated share_plus) were already open; no new duplicates created.

### Notes
- `flutter analyze` reported 61 total findings. Deprecation warnings (#5) and unawaited-try warnings (#4) already tracked; remaining distinct, actionable buckets became #6 (build_context_synchronously, 3 sites), #8 (test-file lints), and #7 (missing tests, 27 files).
- Verified the `throw` usages from the heuristic are compliant: all are in private helpers (`_decryptData`, `_ensureBox`, `_requireBox`) or inside `Result.capture` closures, so no separate error-handling issue was opened for throws.
- `toStringAsFixed` usages were reviewed: all are CSV exports (data, exempt), LLM-facing prompts, or `.d`/debug logging; none are user-facing display, so no i18n-number issue opened.
- No empty catch blocks (`catch (_) {}`) found in lib/.
- Retried gh after a transient connection error; issue #9 created successfully.
