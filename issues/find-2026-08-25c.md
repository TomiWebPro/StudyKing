# Issue Finder Run — 2026-08-25 (run C)

Repo: /home/tomi/StudyKing (TomiWebPro/StudyKing)
Toolchain: ready (flutter analyze clean — no issues).

## Summary
`flutter analyze lib/` reported **no issues**. The repo already had 108 open issues covering most
`Result<T>`, `catch(_)`, `jsonDecode`, `DateTime.parse`, `toStringAsFixed`, `.first`/`.firstWhere`,
and logger-convention defects. This run focused on remaining null-safety / force-unwrap / guarded-list
defects NOT yet represented by the existing 108 titles.

## New issues opened (4)
- **#109** — `extendPlan` crashes on empty `dailyPlans` via unguarded `.last`
  - `lib/features/planner/services/personal_learning_plan_service.dart:758`
- **#110** — `conversation_manager` force-unwraps `['en']!` keyword fallback (crash if 'en' key removed)
  - `lib/features/teaching/services/conversation_manager.dart:370,378` (sibling of #56 in mentor)
- **#111** — `tutor_service` force-unwraps nullable `Result.data!` before `.first` (CastError)
  - `lib/features/teaching/services/tutor_service.dart:381-382`
- **#112** — `ExamResult.fromJson` uses non-nullable `as` casts on optional endTime/subjectId (CastError on partial/legacy records)
  - `lib/features/practice/services/exam_session_service.dart:127-140`

## Duplicates skipped
None — all candidates verified against the open issue list (1–108) and confirmed distinct.

## Candidate patterns checked and found already covered / guarded
- `toStringAsFixed` usages — all remaining are CSV/LLM-facing (en invariant, allowed per AGENTS.md);
  the one user-facing heatmap case is already #99.
- `catch (_) {}` truly empty — none remaining in lib/ (covered by #12/#28).
- `firstWhere`/`first`/`singleWhere` unguarded — covered by #42.
- `DateTime.parse`/`jsonDecode` unguarded — covered by #52/#53/#54/#67/#68/#69/#70.
- `['en']!` mentor keyword — #56 (this run added the teaching-file sibling #110).
