# Issue-finder run — 2026-08-25 (run f)

Scope: convention-deviation + cast-defect audit (flutter analyze clean; 127 issues already open).
The `issues/open/*.md` drafts were verified to already be opened as GitHub issues (#2, #14–#22, #33), so none were re-opened.

## New issues opened (5)

### #128 — lesson_block_model SlideType.values[json['slideType'] as int] crashes (CastError/RangeError)
- bug
- LLM-generated lesson block `slideType` decoded with `as int` + `List[]`; crashes on string or out-of-range value.

### #129 — EvaluationResult.fromJson crashes on malformed conceptBreakdown
- bug
- `(json['conceptBreakdown'] as Map)` / `(v as num)` throw when LLM returns a List/String/stringified number.

### #130 — SourceChunk.fromJson non-nullable 'as int' on chunkIndex crashes ingestion
- bug
- `json['chunkIndex'] as int` throws CastError on missing/null/String field.

### #131 — ExamQuestionResult.fromJson 'as bool'/'as int' casts crash exam load
- bug
- Distinct from #112 (`ExamResult.fromJson`); `isCorrect`/`timeSpentMs` casts throw on partial/legacy records.

### #132 — StudentAvailabilityModel.fromJson 'as String' on blackoutDates crashes
- bug
- `d as String` in blackoutDates map throws CastError on a single non-String entry.

## Duplicates / skipped
- All 12 `issues/open/*.md` drafts already map to open issues (#2, #14–#22, #33) — not re-opened.
- Broad test-coverage gaps (394 source files lacking tests per AGENTS.md table) are covered by umbrella
  test issues (#88, #100, #101, #73, #127); not expanded to per-file spam.
- 127 pre-existing issues cover: jsonDecode/DateTime.parse guards, l10n!/Result swallow, N+1 reads,
  divide-by-zero, firstWhere/.first crashes — none of the 5 new issues overlap these.

## Verification performed
- `flutter analyze` → No issues found.
- Read each reported line to confirm the unguarded `as` / non-nullable cast.
- Cross-checked candidate file/symptom names against `gh issue list` (127 open) — no matches.
