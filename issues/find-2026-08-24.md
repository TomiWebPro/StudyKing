# Issue Discovery Log — 2026-08-24

Run by the ISSUE-FINDER agent. No application code was modified; only this notes file.

## Toolchain
- `flutter analyze`: TOOLCHAIN_READY (`/opt/flutter/.installed` present).
- Current `flutter analyze` result at run time: **1 error** (in a test file) + several info/warning lints (mostly in test files; 3 production `use_build_context_synchronously` infos already tracked by issue #6).

## New issues opened (3)
| # | Title | Label | Summary |
|---|-------|-------|---------|
| 10 | Fix roadmap_adapter_test compilation (undefined MilestoneModelAdapter) | bug | `test/features/planner/data/.../roadmap_adapter_test.dart:17` references `MilestoneModelAdapter` without importing `milestone_adapter.dart`. Break `flutter analyze`. |
| 11 | Move module-level Logger declarations to static final inside classes | enhancement | 12 top-level `final _x = const Logger(...)` vars violate AGENTS.md ("static final at class level"). |
| 12 | Log swallowed exceptions in silent catch(_) blocks | enhancement | 3 `catch (_)` blocks (asr_engine:144, flashcard_generator:123, slide_deck_generator:285) drop the error without logging, violating the no-empty-catch rule. |

## De-duplication
Cross-checked `gh issue list --repo TomiWebPro/StudyKing --state open` (issues #1–#9). Skipped as already covered:
- BuildContext across async gaps → #6
- flutter analyze lints in test files → #8
- missing test files / 27 uncovered sources → #7
- public methods returning Result<T> / no-throw rule → #9
- stale test fakes compilation → #3
- get_syllabus_structure_tool String-as-Map → #2
- non-exhaustive ProcessingStatus → #1
- unawaited Future in try → #4
- share_plus migration → #5

## Notes / non-issues checked
- `toStringAsFixed` usages are only in CSV exports, debug logs, or LLM-facing strings — all permitted by AGENTS.md i18n rules. No issue.
- `.normalized` extension is used correctly; the only `trim().toLowerCase()` is its own definition. No issue.
- Public-method `throw` sites (focus_session_repository, data_backup_service) overlap with the scope of #9. Not reopened.
- No `print(` in production. No production lints beyond the #6 BuildContext items.
- Earlier `flutter analyze` output listed const errors in `concept_map_model_test.dart`; a re-run showed those are now clean (transient state). Only the roadmap_adapter_test error persists.
