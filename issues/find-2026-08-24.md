# Issue Discovery Log — 2026-08-24 (issue-finder pass 8)

Run by the ISSUE-FINDER agent. No application code was modified; only this notes file.

## Toolchain
- `/opt/flutter/.installed` present → TOOLCHAIN_READY.
- `flutter analyze`: 2 issues found (both `use_build_context_synchronously`, already covered by #6).
- `grep` for `TODO`/`FIXME`/`HACK` in `lib/`: none (only ARB string "TODOS LOS INTENTOS").
- `grep` for `toStringAsFixed`: all remaining uses are CSV/LLM/debug-log (permitted by AGENTS.md i18n rule) — no new user-facing violation.
- Test coverage sweep: only `.g.dart` generated files lack tests (expected) — #7 resolved.
- Barrel-file sweep: no unused barrel files (all export-importing files have importers).

## Scan method
- Commissioned a focused subagent sweep of areas NOT covered by the 61 existing issues (localization key sync, secure key service contract, agent loop / memory persistence, llm cache, unguarded `.last`/`[0]`/`~/`, `print(`, duplicate l10n sources).
- Manually verified each candidate with file:line citations before filing.

## New issues opened (5)

| # | Title | Label |
|---|-------|-------|
| 62 | LlmResponseCache.get/invalidate throw on corrupt or non-Map Hive records | bug |
| 63 | LlmResponseCache grows unbounded (no max-size / no proactive TTL sweep) | bug |
| 64 | SecureApiKeyService violates Result<T> contract and hides KeyringLocked failures | bug |
| 65 | AgentMemory.storeSessionSummary orphans session blobs after list trim (unbounded growth) | bug |
| 66 | Dual l10n source (l10nProvider) produces stale-language provider-generated text after language switch | bug |

## De-duplication
Cross-checked `gh issue list --repo TomiWebPro/StudyKing --state open` (#1–#61). Skipped as already covered:
- Parsing crashes, force-unwrap `['en']!`, Result<T> conversion, inline Logger, empty catches, build-context-async, share_plus, unawaited, switch exhaustiveness, test coverage (#7 now resolved), Hive migration/plaintext/API-key storage (#37/#38 — distinct from this service's Result/contract bug), N+1, hardcoded dashboard i18n (#39 — distinct from dual-source l10n desync), firstWhere/first (#43), LLM chat JSON parsing (#52), LlmUsageMeter corrupt record (#51), LlmTaskManager loadFromBox crash (#57), orphaned tasks (#60).
- The 5 new issues target distinct subsystems/mechanisms (llm_response_cache corrupt-read + unbounded growth, secure key service contract, agent memory orphan growth, dual l10n source of truth) not present in #1–#61.

## Notes
- Capped at 5 new issues to avoid spam. Only this notes file is committed; no application code modified.
